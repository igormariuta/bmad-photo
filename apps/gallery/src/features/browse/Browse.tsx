/**
 * Placeholder Browse panel (Story 3.1) — the Photo Grid (Story 3.2) and
 * Facet Panel (Story 3.3) replace this content; this story only wires up
 * the tab mechanism and the AD-3 import boundary.
 */
export function Browse() {
  return (
    <div className="mx-auto max-w-article-max px-gutter py-8">
      <p className="text-eyebrow text-accent uppercase">// BROWSE</p>
    </div>
  );
}
