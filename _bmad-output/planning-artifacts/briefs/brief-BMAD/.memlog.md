---
topic: Monorepo product suite: iOS camera-app landing + EXIF photo gallery/sorter + shared Brutalist-mono design system
updated: 2026-07-02T15:26
---

- (event) Create intent. Seeded from brainstorm-intent (brainstorm-monorepo-design-system): architecture spine is well-defined; product side (users/problem/value) is thin and to be drawn out. Pet project to exercise BMAD end-to-end -> right-size, low stakes
- (decision) Working mode: Fast path
- (decision) Camera app core value: (1) kill iOS non-disableable HDR; (2) strip native processing incl over-sharpening; (3) own preset system on a clean base — presets stay valuable even if HDR later becomes disable-able
- (decision) Preset features: shadow/highlight curve; color mixer (per-color mute/boost + hue shift, e.g. yellow->orange/green, Lightroom-HSL-like); extensible tools; possible preset SHARING (Fuji-recipe-like community)
- (decision) Camera target user: people who dislike the iOS HDR/over-sharpening and want a clean base + creative preset control
- (decision) EXIF companion scope: client-side analytics + filtering — sort/analyze by lens/focal length, preset (if written to EXIF), date, location; upload from phone or computer
- (decision) EXIF REJECTED: backup/file store + live iOS photo sync — too painful, would need a separate native app. Kept out of scope; tool stays analysis/filter only
- (decision) Camera<->gallery link deferred; proposed connective tissue = camera writes preset name into EXIF/XMP so gallery can analyze preset usage without live sync
- (assumption) Camera app is native iOS, built on a SEPARATE track; THIS monorepo's buildable scope = landing (Astro) + gallery (Vite) + shared design system. Landing markets the (real/planned) camera app
- (event) Coach to propose EXIF framing: personal shooting-habits insights dashboard, client-side/privacy (no upload to server), preset-in-metadata as camera<->gallery bridge
- (decision) CONFIRMED A-D: (A) camera = native iOS, outside monorepo; (B) EXIF gallery = primary buildable web product; (C) EXIF = client-side + insights, backup/sync out of scope; (D) no monetization/deadline, goal is to exercise BMAD
- (decision) Finer defaults (fast-path, correctable): preset->EXIF/XMP bridge = design the contract conceptually, DEFER implementation; camera app = narrative for landing, NOT built in this BMAD exercise; location = privacy-first, optional place grouping
- (event) Finalized: doc-standards (structure+prose) applied light-touch to brief.md + addendum.md; status draft->ready. No external handoffs configured
