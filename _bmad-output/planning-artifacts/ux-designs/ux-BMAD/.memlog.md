---
topic: Monorepo Product Suite UX — EXIF Gallery, Landing, shared Brutalist-mono Design System
updated: 2026-07-06T11:25
---

- (event) Imported NOT LAZY blog's live Brutalist-mono design system as design foundation: imports/notlazy-design-guide.tsx (full component storybook), notlazy-tokens.css (color tokens light/dark), notlazy-mono-layer.css (utility classes), notlazy-helpers.tsx (Section/Panel/Spec layout helpers). Source: lazy-team/lazy-blog-front repo, src/app/brand/ and src/shared/ui/theme/.
- (decision) User directive: reuse the NOT LAZY Brutalist-mono system as the visual identity foundation for this monorepo's shared Design System (packages/theme, packages/ui) — inherit tokens/type/geometry, adapt/extend components for Gallery (Insights, Facets, Ingest) and Landing (hero, pillars, preset showcase) needs.
- (decision) Insights charts: extend StatBar's block-cell (████░░░░) language into horizontal distribution histograms — one row per bucket (focal length, ISO, shutter, aperture, megapixel mix, selfie/rear ratio, time-of-day-by-hour). No new chart primitive family; stays in the ASCII/terminal idiom.
- (decision) Facet filtering UI: sidebar panel (desktop) / slide-up sheet (mobile) of Select + RadioGroup + range controls, reusing existing form components almost as-is.
- (decision) Gallery includes a browseable photo grid/thumbnail view in addition to Insights — filters narrow both. Tap a photo opens a detail view with its full Metadata. Confirms/extends PRD FR-8 and UJ-1 resolution beat.
- (decision) Landing: single-scroll static page (Hero -> 3 pillars -> preset showcase as static before/after imagery/diagrams -> coming-soon footer). No interactive JS demo in v1; Astro SSG stays minimal-JS.
- (decision) Gallery nav: UnderlineTabs switches Insights <-> Browse (Grid); filter sidebar/sheet persists across both tabs.
- (decision) Photo detail opens in a Modal overlay (reuses existing Modal shell) showing the photo + its full Metadata list; grid stays scrolled in place underneath.
- (decision) Ingest progress: determinate StatBar-style 'Parsing 42/100' with filling block-bar, since batch size (<=100) is known upfront.
- (decision) First-run empty state: full-screen centered prompt (eyebrow + headline re: privacy/nothing-uploads promise + single 'Add photos' primary Button opening the photo picker), no nav chrome until photos are Ingested.
- (decision) Keep the NOT LAZY light/dark theme toggle on both Gallery and Landing (same .dark class mechanism, both token sets inherited as-is).
- (decision) Photos with unreadable/stripped Metadata are excluded from the grid and Insights math, surfaced instead as a visible 'N unreadable' count/InfoBox near the Insights header (FR-6).
- (decision) Both apps share a minimal fixed header: wordmark + theme toggle only, no real navigation (mirrors NOT LAZY header pattern) -- reinforces one-suite identity per SM-2.
- (event) DESIGN.md and EXPERIENCE.md drafted (status: draft) -- DESIGN.md inherits NOT LAZY tokens/type/geometry wholesale + 2 new component families (histogram-bar, facet-panel); EXPERIENCE.md closes IA for Gallery (empty-state -> Ingest -> Insights/Browse tabs -> photo detail modal) and Landing (single scroll: hero -> pillars -> preset showcase -> coming soon), mirrors PRD UJ-1/UJ-2 as Key Flows verbatim.
- (assumption) Non-blocking assumptions carried into EXPERIENCE.md: (1) a persistent 'Add more photos' affordance exists once the set is non-empty though PRD doesn't specify re-Ingest UI; (2) selections beyond the 100-photo cap are rejected with a message, not silently truncated; (3) no bulk-select/bulk-action on the photo grid in v1; (4) non-content-capturing analytics is permitted (PRD only bans analytics that capture image content); (5) exact grid column-count breakpoints left to implementation, governed by the fixed 28px gap / square-cell rule.
- (event) Rubric walker ran (review-rubric.md): 1 critical (Landing Hero/Pillars/Preset-showcase had zero component specs), 1 high (Facet-panel range facets had no backing component), 3 medium, 4 low. All critical/high/medium fixed: added hero, pillar-card, preset-comparison, range-control, theme-toggle to DESIGN.md.Components + matching EXPERIENCE.md Component/State Pattern rows; reconciled histogram-bar's FR-7 dimension list (dropped exposure-comp, an FR-8 Facet-only field); softened the dead Accessibility Floor contrast pointer. Low findings also fixed: token-vs-prose consistency in Layout & Spacing, committed Unreadable-count to InfoBox (dropped 'or Stat'), added UJ-2 failure cross-link, added permission-denied [ASSUMPTION] note.
- (event) Key-screen mocks rendered and promoted .working/ -> mockups/: gallery-empty-state.html, gallery-insights.html, landing-hero.html. Inline-linked from EXPERIENCE.md IA sections. Spine-only (no mock, tables suffice per user's 3-screen pick): Ingest progress, Browse grid, Photo-detail modal, mobile Facet-panel sheet, Preset-comparison, Coming-soon footer.
- (event) Editorial polish applied: structural pass found the pair already lean (fixed 2 raw-pixel/token consistency spots); prose pass found and fixed one real defect -- Brand & Style claimed 'exactly two new visual ideas' but Components had grown to 6 new ones after rubric fixes; rewrote to name all of them accurately.
- (event) spines finalized
- (change) USER FEEDBACK after finalization: Insights should be pure statistics with NO filtering UI; filtering belongs only on Browse, where each photo grid cell must show an EXIF badge (Dot-separated caption, e.g. '24mm . f/1.8 . ISO 200') for at-a-glance reading of filtered results, not just a tap-to-open modal. This revises PRD Section 9's own [ASSUMPTION] that filters narrow both photo list and Insights.
- (change) Applied: Facet-panel + range-control moved to Browse tab only (removed from Insights); histogram-bar/Insights now always computed over the full readable set; photo-grid-cell extended with an EXIF-badge caption row. Re-generated mockups/gallery-insights.html (facet-panel removed, full-width stats-only layout) and created new mockups/gallery-browse.html (Facet-panel + photo grid with EXIF badges).
- (change) USER FEEDBACK: Landing felt too plain; wants lightweight CSS animation in the product itself (not just the mockup). Decision: reinstate GlitchText (previously excluded in reconciliation) for the Landing hero headline only, adapted to a ONE-SHOT settle-in beat (not the source's continuous auto-beat) to respect the suite's own 'motion is a single restrained gesture, never loops' rule. Added CSS-only staggered scroll-reveal (fade-up, animation-timeline:view()) to pillar-card and preset-comparison. All motion respects prefers-reduced-motion and is progressive enhancement -- content never gated behind it.
- (event) Applied to DESIGN.md (Brand & Style motion paragraph, hero/pillar-card/preset-comparison component notes, GlitchText reinstated in inherited list, Do's/Don'ts row), EXPERIENCE.md (Hero/Pillar-card/Preset-comparison Component Pattern rows, Interaction Primitives motion note, Accessibility Floor reduced-motion line), reconcile-notlazy-design-system.md (reversed the GlitchText exclusion), and mockups/landing-hero.html (real CSS keyframe animations added: heroGlitchSettle, revealUp, scrollRevealUp, all with a prefers-reduced-motion override).
- (change) Architecture phase (ARCHITECTURE-SPINE.md AD-7) resolved two open [ASSUMPTION] tags left here: repeat-Ingest ('Add more') appends and dedupes rather than replacing, and the 100-photo cap is cumulative per-session (not per-batch). Updated the State Patterns 'Batch exceeds 100' row and the Interaction Primitives 'Photo picker' bullet to point to AD-7 instead of carrying their own assumption text.
