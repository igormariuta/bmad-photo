---
topic: Monorepo: iOS-camera landing + EXIF gallery + shared design system/component library
goal: Technical foundation: refine the monorepo, component sharing, tooling
mode: partner
status: complete
updated: 2026-07-01T12:21
---

- (decision) Mode: Creative Partner. Goal: technical foundation for the monorepo. Techniques: Morphological Analysis -> TRIZ Contradiction -> Backcasting
- (technique) started Morphological Analysis
- (decision by user) Axis A structure: feature-based across all projects
- (decision by user) Axis C versioning: always latest within the repo; the lib is shared externally separately (publishable)
- (decision by user) Axis D tokens: Tailwind with a custom theme
- (direction by user) Axis F: leaning toward micro-frontend — shared host + 2 apps (landing, gallery), one route; prefers plain React, dislikes all-SSR, unsure about MFE setup
- (question by user) Open: B (Nx vs alternatives?), E (headless+styled vs ready-made components given a shared Tailwind theme?), is Next needed for the landing?
- (decision by user) Axis F RESOLVED: pure monorepo without a micro-frontend — two independent apps + a shared lib via workspace
- (insight by coach) Consequence: heterogeneous monorepo — Next(SSG) landing + Vite-React(SPA) gallery; the lib must be framework-neutral; E = headless+styled via a Tailwind preset in the lib
- (decision by user) KEY CONSTRAINT: this is a pet project for testing the BMAD architecture; wants to refine tooling but not go too deep into the weeds -> minimize friction
- (decision by user) Axis C FINAL: lib is publishable — versioning via changesets, optionally deployed as an npm package, more universal; B=Turbo+pnpm confirmed
- (insight by coach) Morphological Analysis closed: A feature-based / B Turbo+pnpm+changesets / C publishable+versioned / D Tailwind theme / E headless+styled+preset / F pure monorepo, 2 apps / G SSG-landing+CSR-gallery
- (technique) started TRIZ Contradiction
- (idea by user) What's shared: brand colors, spacing rhythm (within/between sections), consistent fonts+sizes, overall look&feel, header style, hover, loader, labels, modals, buttons, visual effects, some charts
- (idea by user) Landing: home is a bit more unique; privacy/about pages — consistent shared style. Gallery uses MORE design-system components (forms, etc.)
- (insight by user) CORE FEAR: desync -> two UI systems sharing only colors. Wants one cohesive system, not two UIs
- (insight by user) Even the home-hero stays within one system (shared fonts/colors/spacing). Landing's uniqueness = extra visual elements that still consume the same tokens but aren't needed anywhere else -> no point moving them into the lib
- (insight by coach) TRIZ RESOLVED -> Placement rule: tokens are universal and enforced (consistency by construction); lib membership = PURELY a reuse decision (2+ places -> lib; one-off -> stays in the app, but token-driven). Reuse is orthogonal to consistency
- (direction by user) User preference: write all _bmad-output content (memlog + artifacts) in English from now on; conversation stays in Russian
- (technique) started Backcasting
- (idea by user) Add Storybook to the lib — to view components and their states in isolation
- (decision by user) REVERSAL of axis C: for the pet-project stage, DROP lib versioning/publishing; simplify to direct workspace import (latest-internal only). Flow: build component+states -> verify in Storybook -> import into app -> use
- (decision by user) Arbitrary-value ban: keep it, BUT standard Tailwind utility classes must stay usable — ban only bracket/arbitrary values (bg-[#hex], mt-[13px]), allow the full token-mapped utility vocabulary
- (decision by user) TRUE first-move sequence (Backcasting payoff): 1) define theme tokens FIRST — colors/fonts/spacing/rhythm/style, seeded from his existing drafted theme; 2) first primitives — Button, Input, Header+nav; 3) bring up gallery; 4) bring up landing LAST
- (question by user) Open: Astro vs Next for the SSG landing? Next feels like a massive framework for a mostly-static marketing page
- (idea by user) Design system aesthetic: Brutalist mono — raw, high-contrast, monospace type, visible structure, hard edges, minimal ornament
- (insight by coach) Brutalist-mono REINFORCES the architecture: tight rigid token scale fits brutalism, so no-arbitrary-value lint becomes the AESTHETIC not just discipline; single mono typeface simplifies theme; minimal-JS/no-fancy-anim aligns with Astro zero-JS islands. The constraint IS the style — aesthetic and architecture agree
- (decision by user) Landing framework LOCKED: Astro (SSG, zero-JS islands, React islands consuming shared ui) instead of Next — lighter, homogeneous Vite toolchain with the Vite gallery, fits Brutalist-mono minimal-JS
- (idea by user) DS FOUNDATIONS: colors, typography, geometry, a few layout tokens (spacings+rhythm, letter-spacing, line-height)
- (idea by user) PRIMITIVES - Buttons: variants primary/outline/danger; states default+disabled; icon-only (no text) button; button-with-loader
- (idea by user) PRIMITIVES - Forms: input, textarea, select, switch, checkbox, radio, labels + field errors
- (idea by user) PRIMITIVES - Data: progressbar, EXIF categories, possibly charts
- (idea by user) PRIMITIVES - Nav/layout: relatively shared Header; ~unified content width across platforms
- (idea by user) PRIMITIVES - Overlays/feedback: modals, notification toasts (default+danger), global spinner/loader; ~shared 404/error page look
- (decision by user) Session can wrap here; gallery/EXIF-specific components decided later as needs emerge
- (insight) SYNTHESIS: The session's spine is one idea — a single UI system enforced by tokens, where lib-membership is a pure reuse decision (orthogonal to consistency). Every later choice collapses into it: Brutalist-mono makes the rigid token scale the AESTHETIC (no-arbitrary-value lint = style, not discipline); minimal-JS brutalism justifies Astro zero-JS islands; homogeneous Vite toolchain (Astro+Vite) fits the minimal-friction pet-project goal; theme-first build order flows from tokens-as-source-of-truth. Chosen direction: pnpm+Turbo monorepo, packages/{theme,ui,eslint-config,tsconfig}, apps/{landing(Astro SSG), gallery(Vite SPA)}, workspace-internal (no publish for now), Storybook-driven component loop. Component detail + exact spacing scale deferred to UX (bmad-ux).