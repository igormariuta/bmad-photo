---
baseline_commit: 56af8b493196cf88285544600cca4585d3355d92
---

# Story 3.5: Photo Detail Modal

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As the builder,
I want to tap a photo to see its full metadata,
so that I can inspect exactly what was captured.

## Acceptance Criteria

1. **Given** a photo grid cell, **when** the user taps it, **then** a Photo-detail-modal opens, reusing the inherited `Modal`/`ModalHeader` shell. [Source: planning-artifacts/epics.md#Story 3.5]
2. **Given** the modal is open, **when** its body renders, **then** it lists every extracted Metadata field as Spec (label/value) rows — the badge's three fields plus shutter speed, exposure compensation, capture timestamp, megapixel mode, and front/rear. [Source: planning-artifacts/epics.md#Story 3.5]
3. **Given** the modal is open, **when** the user presses Esc, clicks the backdrop, or clicks the close control, **then** it closes, using the same focus-trap and `aria-labelledby` wiring as every other Modal in the system. [Source: planning-artifacts/epics.md#Story 3.5]
4. **Given** a grid cell, **when** it is rendered, **then** it is `<button>`-semantic with an `aria-label` naming at least the photo's capture date. [Source: planning-artifacts/epics.md#Story 3.5, #UX-DR15]

## Dev Notes

- **Depends on Story 3.2** (`PhotoGridCell` — its `onOpen` prop was left a no-op there specifically for this story to wire up) and Story 1.5 (`Modal`/`ModalHeader`).
- **AC #4 is already built** — Story 3.2 already made the cell `<button>`-semantic with a `capturedAt`-derived `aria-label` (including the missing-field fallback phrasing from that story). This story only needs to wire the actual `onClick`/`onOpen` handler; don't rebuild the button semantics.
- **`Spec` (label/value row) doesn't exist anywhere yet and is Gallery-local, single-consumer** — same situation as `Panel` was before Story 2.4 (which needed it in ≥2 places and built it shared), but `Spec` has exactly one consumer in this whole project (this modal), so per FR-2 it stays local, not `packages/ui`. It's a small presentational component; reference implementation (from the same demo-helpers source that also had `Panel`):
  ```tsx
  export function Spec({ label, value }: { label: string; value: string }) {
    return (
      <div className="grid grid-cols-[1fr_auto] items-baseline gap-6 border-b-2 border-[var(--m-dim)] py-3">
        <div className="text-[14px] text-[var(--m-fg)]">{label}</div>
        <div className="text-[12px] tracking-[0.06em] whitespace-nowrap text-[var(--m-accent)] tabular-nums">{value}</div>
      </div>
    );
  }
  ```
- **8 Spec rows total**, each needing its own display formatting (none of this is specified upstream beyond the field list — reasonable defaults, flag if a different format is wanted):
  - `lensLabel` (already `"24mm"`-formatted)
  - `apertureF` → `f/{value}`
  - `iso` → `ISO {value}`
  - `shutterSpeedSec` → for values `< 1` (the vast majority — phone shutter speeds are almost always sub-second), convert to a fraction: `1/{round(1/value)}s` (e.g. `0.008` → `"1/125s"`, matching how photographers actually read shutter speed). For values `>= 1` (a real but rare long-exposure case), display as a plain decimal-seconds string instead (e.g. `2` → `"2s"`), not a nonsensical `"1/1s"` — the fraction formula only makes sense below 1 second
  - `exposureCompEv` → signed EV, e.g. `+0.3 EV` / `-1.0 EV` / `0 EV`
  - `capturedAt` → human-readable (e.g. `"14 Jun 2026, 18:32"`), reading the timezone-naive ISO-8601 string as-is (per Story 2.2's `capturedAt` decision — no timezone conversion, since none is known)
  - `megapixelMode` → `12MP` / `48MP`
  - `camera` → `Front` / `Rear`
  - **Reuse Story 3.2's missing-field placeholder convention** (e.g. `"—"`) for any of these that are `undefined` on a given photo — consistency with the grid badge, not a new pattern
- **Modal wiring is a direct application of Story 1.5's `Modal`/`ModalHeader` contract** — `isOpen`, `onOpenChange`, `labelledBy` matching `ModalHeader`'s `titleId`, render-prop children receiving `close`. Nothing new to invent here; this story is populating that shell's body, not extending the shell itself.

## Tasks / Subtasks

- [x] Task 1: `Spec` component (AC: #2) — `apps/gallery/src/features/photo-detail/Spec.tsx`
  - [x] Implement per Dev Notes' reference shape
- [x] Task 2: Photo-detail-modal (AC: #1, #2, #3) — `apps/gallery/src/features/photo-detail/PhotoDetailModal.tsx`
  - [x] `Modal` + `ModalHeader` (Story 1.5), title naming the photo (e.g. its capture date or lens), body = 8 `Spec` rows per Dev Notes' formatting rules and missing-field fallback
  - [x] Standard close behavior inherited from `Modal` itself (Esc/backdrop/close-control, focus trap, `aria-labelledby`) — no extra work needed here beyond correct `labelledBy`/`titleId` wiring
- [x] Task 3: Wire the grid cell trigger (AC: #1)
  - [x] `PhotoGridCell`'s `onOpen` (Story 3.2, previously a no-op) now opens this modal for the tapped photo
- [x] Task 4: Verify (AC: #1–#4)
  - [x] Tap a grid cell — confirm the modal opens with all 8 fields correctly formatted, including a photo with at least one missing field showing the placeholder, not `"undefined"`
  - [x] Confirm Esc, backdrop click, and the close control all dismiss the modal
  - [x] Confirm the grid cell's `aria-label`/button semantics (already built in 3.2) are intact

### Review Findings

3-layer adversarial code review (Blind Hunter, Edge Case Hunter, Acceptance Auditor) against the uncommitted working-tree diff (baseline `56af8b4`, matching this story's own `baseline_commit`) — the diff also spans Story 2.2's ingest/worker/HEIC pipeline and Story 3.3's Facet-panel slider work, per the "retroactively reattributed" rounds already noted above; findings below are judged primarily against this story's own AC/scope.

- [x] [Review][Decision] Story-attribution/documentation-consistency gap in the retroactive scope cleanup — <the "move out-of-scope rounds to their real owning story" cleanup was applied to rounds 16–21 (→ Stories 2.2/3.3) but not consistently elsewhere: the entire Insights/Story-2.4 rewrite (rounds 4, 6–15 — `Insights.tsx`, `aggregations.ts`, `HistogramBar.tsx`, new `Sparkline`/`Panel.caption` features) stays attributed to this story with no corresponding Story-2.4 artifact entry; round 4's `computeInsights` reorder is a documented deliberate deviation from Story 2.4's own accepted AC, left with no trace in that story's own file; one of two `FacetPanel.tsx` edits from this session (round 4) stayed here while the other (round 9) was moved to 3-3; cross-cutting `packages/ui` fixes (`StatBar.tsx` rounding, `HeaderBar.tsx` border) have no backfill note in their real owning stories. Doesn't affect runtime correctness, but the sprint's documentation/acceptance record has silently diverged from the shipped code.> — **Resolved by user (2026-07-08): accept as-is, no backfill.** The full change history is already preserved in this story's own Dev Agent Record/Change Log; Story 2.4 is already `done` with no open artifact expecting an update.
- [x] [Review][Patch] Detail modal shows a permanently-broken image for photos whose media hasn't finished loading yet [apps/gallery/src/features/browse/Browse.tsx:39,65,69] — fixed: `Browse.tsx` now tracks `selectedPhotoId` and derives the modal's photo live from `useReadablePhotos()` each render instead of a captured snapshot
- [x] [Review][Patch] "Add more"/"Add folder" re-enable mid-batch, defeating AddMoreControl's own documented re-entrancy guard [apps/gallery/src/store/ingestStore.ts:181-199 (mergeCommit), apps/gallery/src/features/ingest/ingestPhotos.ts:146-149] — fixed: new `parsing` store flag (set on `beginIngest`, cleared by new `finishIngestBatch()` on the worker's own `"complete"` message), `AddMoreControl` now gates on `useIsBatchParsing()` instead of `useIsIngestComplete()`
- [x] [Review][Patch] Shutter-speed formatting divides by zero when `shutterSpeedSec === 0`, rendering `"1/Infinitys"` [apps/gallery/src/features/photo-detail/PhotoDetailModal.tsx formatShutterSpeed; apps/gallery/src/features/browse/FacetPanel.tsx formatShutterSpeed; apps/gallery/src/features/insights/aggregations.ts formatShutterLabel] — fixed: all 3 formatters guard `value > 0 && value < 1` for the fraction branch
- [x] [Review][Patch] Detail-modal photo `<img>` has `alt=""`, hiding the story's own primary content from assistive tech [apps/gallery/src/features/photo-detail/PhotoDetailModal.tsx:134-139] — fixed: `alt={photo.fileName}`
- [x] [Review][Patch] Folder/file picks resolving to zero image files silently no-op with no user feedback [apps/gallery/src/features/ingest/EmptyState.tsx:39-41, apps/gallery/src/features/ingest/AddMoreControl.tsx:48-50] — fixed: both now set `limitMessage("No photos found in that selection.")`
- [x] [Review][Patch] Stale doc comment: `isImageFile` says it works around "the same gap `isHeic` above" but `isHeic` is defined below it [apps/gallery/src/features/ingest/ingestPhotos.ts:8-17] — fixed: "above" → "below"
- [x] [Review][Patch] Insights renders a bare eyebrow + summary line with no explanation when every filtered photo has zero data across all 5 dimensions [apps/gallery/src/features/insights/Insights.tsx:106-126] — fixed: added a "No data for this batch." fallback when `dimensions.length === 0`
- [x] [Review][Patch] User-reported live bug (not from the 3-layer review, found during this same session): `SingleSlider`'s track rendered an accent-colored fill from the left edge to the thumb, implying a "selected range from zero" that a single picked value doesn't have [apps/gallery/src/features/browse/RangeSlider.tsx:271-275] — fixed: removed the accent fill div from `SingleSlider`'s track, leaving only the neutral `bg-dim` track (the thumb alone stays accent-colored); live-verified via Playwright screenshot
- [x] [Review][Defer] Load-bearing dependency on `heic2any` (pinned `^0.0.4`, pre-1.0, documented non-reentrant-WASM bug already worked around, no fallback decoder) [apps/gallery/package.json, apps/gallery/src/features/ingest/ingestPhotos.ts] — deferred, pre-existing; needs a deliberate vendor-evaluation decision, not a quick patch
- [x] [Review][Defer] Worker→main-thread file/message pairing relies on strict sequential ordering with no id carried in the message to verify it [apps/gallery/src/features/ingest/ingestPhotos.ts:136-164] — deferred, pre-existing; correct today given the worker's guaranteed sequential processing, but silently fragile against any future move toward parallel parsing
- [x] [Review][Defer] Progressive per-photo commits mean up to ~2×N store updates/re-renders for an N-photo batch instead of one batched commit [apps/gallery/src/features/ingest/ingestPhotos.ts, apps/gallery/src/store/ingestStore.ts] — deferred, pre-existing; inherent trade-off of the explicit user-requested progressive-loading redesign (round 3), unmeasured cost for large batches
- [x] [Review][Defer] Heavy iterative churn on shared `packages/ui` components (Modal, ModalHeader, Panel, StatBar, HeaderBar, UnderlineTabs, Sparkline) verified only via this app's own Playwright checks, no evidence `@bmad/landing` (the other real consumer) was checked against the new props/variants [packages/ui/src/**] — deferred, pre-existing; needs a cross-app visual pass, not a code patch

## Project Structure Notes

```text
apps/gallery/src/features/photo-detail/
  Spec.tsx               # new — Gallery-local, single consumer
  PhotoDetailModal.tsx    # new — Gallery-local
```

This matches the Architecture's structural seed (Story 1.1), which reserves a separate `features/photo-detail/` folder for exactly this — "Modal + full Metadata Spec rows" — distinct from `features/browse/` where `PhotoGridCell` (Story 3.2) lives. `PhotoDetailModal` imports `PhotoGridCell`'s `onOpen` callback wiring from `browse/`, but its own files belong in `photo-detail/`.

### References

- [Source: planning-artifacts/epics.md#Story 3.5, #UX-DR8, #UX-DR15] — acceptance criteria, field list
- [Source: architecture/architecture-BMAD/ARCHITECTURE-SPINE.md#AD-4] — `Photo` field types this modal formats for display
- [Source: 3-2-photo-grid-browse-unfiltered-view.md] — `PhotoGridCell`'s `onOpen` prop and missing-field placeholder convention this story reuses
- [Source: 1-5-shared-component-library-display-feedback-navigation-primitives.md#Task 7] — `Modal`/`ModalHeader` contract

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- Live Playwright verification surfaced a real pre-existing bug in `packages/ui/Modal.tsx` (Story 1.5): the full-viewport flex-centering wrapper is painted *after* the backdrop div and is also `absolute inset-0`, so it — not the backdrop — receives every click outside the dialog. The backdrop's own `onClick={close}` never fires. Confirmed two ways: a raw corner click (5,5) did nothing, and Playwright's own center-point click reported "subtree intercepts pointer events" from the centering wrapper. Fixed by moving `onClick={close}` from the backdrop div to the centering wrapper (the dialog's existing `stopPropagation` still protects in-dialog clicks) — a 1:1 swap, no new behavior invented, and in scope since AC #3 explicitly requires backdrop-click-to-close to work. This is a shell-level fix benefiting every `Modal`/`ConfirmModal` consumer, not something specific to this story's body content.
- `formatCapturedAt` initially destructured `capturedAt.split("T")`/`.split("-")`, which fails `tsc --noEmit` under this repo's `noUncheckedIndexedAccess` (array destructuring types every element as possibly `undefined`). Switched to fixed-offset `.slice()` reads instead, matching `aggregations.ts`' existing `bucketHour` convention for parsing this same timezone-naive `capturedAt` shape.
- No React component-rendering test infra exists in this repo (a repeated, deliberate convention from prior stories, e.g. 3.2/3.4's deferred "no component-render test infra") — followed precedent: unit-tested every pure formatting function and `formatSpecRows`' full 8-row assembly, verified actual rendering live instead.
- Live verification: generated 4 synthetic JPEGs via Python/Pillow+piexif (full metadata w/ sub-second shutter+positive EV+rear camera+12MP; long-exposure w/ negative EV+front camera+48MP; several fields deliberately omitted to exercise the placeholder path; one with no EXIF at all to confirm it's excluded as unreadable), started the Vite dev server, drove the real ingest → 3 readable photos gridded (4th correctly excluded as unreadable) → tapped each grid cell and confirmed all 8 Spec rows render with the exact expected formatting per photo, including the missing-field photo showing `"—"` (never the literal `"undefined"`) and its modal title falling back to `"Photo details"` since `capturedAt` was itself missing. Confirmed Esc, backdrop click, and the close-control button all dismiss the modal (backdrop only after the Modal.tsx fix above), and `aria-labelledby` correctly resolves to the rendered title element. Zero console errors throughout.

### Completion Notes List

- `Spec.tsx` (Gallery-local, single-consumer): implemented per Dev Notes' reference shape, but using this app's existing token utility classes (`text-body`, `text-caption`, `border-dim`, `text-fg`, `text-accent`) instead of the reference's raw `var(--m-*)` inline values — the token utilities already exist and are the established convention (e.g. `HistogramBar.tsx`), and raw Tailwind arbitrary values (`text-[14px]`, `grid-cols-[1fr_auto]`) are blocked by this repo's `tailwindcss/no-arbitrary-value` lint rule. The `1fr auto` grid-template uses an inline `style` instead, matching `HistogramBar.tsx`'s own precedent for the same lint constraint.
- `PhotoDetailModal.tsx`: exports one pure formatting function per field (`formatShutterSpeed`, `formatExposureComp`, `formatCapturedAt`, `formatMegapixelMode`, `formatCamera`) plus `formatSpecRows` which assembles all 8 rows — mirrors `PhotoGridCell`'s `formatExifBadgeSegments` pattern (pure, exported, unit-tested without rendering) and directly reuses `formatExifBadgeSegments` for the shared lens/aperture/ISO fields rather than reimplementing that formatting. `MISSING_FIELD_PLACEHOLDER` was exported from `PhotoGridCell.tsx` (previously module-private) so both files share the one `"—"` constant instead of duplicating it.
- Modal title uses the formatted `capturedAt` (e.g. `"14 Jun 2026, 18:32"`), falling back to `"Photo details"` when `capturedAt` itself is missing — Dev Notes left the exact title content as a reasonable-default choice ("e.g. its capture date or lens").
- `Browse.tsx` gained `selectedPhoto` state (`useState<Photo | null>`) and renders one `PhotoDetailModal` instance; each `PhotoGridCell`'s `onOpen` now sets `selectedPhoto` to that cell's photo. `PhotoGridCell.tsx`'s stale "unused/no-op for this story" doc comments were updated to reflect the real wiring.
- Found and fixed a real pre-existing bug in `packages/ui/Modal.tsx` (see Debug Log) — backdrop-click-to-close was non-functional for every Modal/ConfirmModal consumer in the app, not just this story's new modal.
- turbo lint/build/test all pass (94/94 Vitest tests, 17 new); live-verified via Playwright against synthetic Pillow-generated EXIF fixtures (3 readable + 1 unreadable photo) — all 8 Spec rows, the missing-field placeholder path, the title fallback, and all three close mechanisms confirmed working, zero console errors.
- **Round 2 (user design feedback, 2026-07-08)**: user rejected the plain-list layout and asked for a two-column redesign — photo at 3/4 width, metadata at 1/4 width — triggering the UX designer persona (Sally) for the visual spec before implementing. Design decisions (both confirmed with the user before coding): (1) `Modal` (`packages/ui`) gained a `variant?: "default" | "wide"` prop rather than a one-off wrapper, since a wide/media-flavored modal variant is a reusable shell concern, not something specific to this story's body; `"wide"` swaps `max-w-md`→`max-w-5xl` and drops the built-in `px-9`/padding wrapper so callers can lay out edge-to-edge. (2) `Spec` rows changed from side-by-side (label left, value right) to stacked (label above value) — reads better in a narrow 25%-width column; restyled with `text-data-label`/`text-body` tokens (matching `Stat.tsx`'s existing label/value convention) instead of the round-1 `text-caption`+inline-style grid-template. The 3:1 split itself is `grid-cols-4` + `col-span-3`/`col-span-1` (not an arbitrary `fr` ratio) to stay clear of the `tailwindcss/no-arbitrary-value` lint rule; collapses to a single stacked column below `lg`. The photo `<img>` (previously never rendered inside the modal at all — round 1 only showed metadata text) is capped with `max-h-screen object-contain` and the container has no forced height, so the dialog's height follows the photo's natural rendered size, not the viewport.
- **Also fixed during round 2** (user-reported, screenshot showing a large empty box under the Facets sidebar): `App.tsx`'s Facet-panel `<aside>` was stretching to match its flex-row sibling's height (the tab content column, which fills leftover viewport space) instead of sizing to its own content — default flexbox `align-items: stretch`. Added `self-start` to the `<aside>` alone, leaving the main content column's sizing untouched.
- turbo lint/build/test re-verified clean across `@bmad/gallery`, `@bmad/ui`, and `@bmad/landing` (the other `@bmad/ui` consumer) after both round-2 changes; live-verified via Playwright at both desktop (1560×1000, confirmed the 3:1 grid split's rendered proportions and all three close mechanisms) and mobile (500×900, confirmed the stacked fallback) viewports, plus a full-page screenshot confirming the Facets sidebar now hugs its own content. Zero console errors.
- **Round 3 (user spacing feedback, 2026-07-08)**: user asked for breathing room around the photo using "the standard section spacing from the design system," a slightly wider (~5-10%) detail panel, and more internal padding on that panel. Photo column gained `p-section-rhythm` (the design system's `--m-space-section-rhythm`/40px token — used for section-level rhythm elsewhere, this is its first use as a component-level padding, chosen because the user explicitly named it). The 3:1 split (75/25) became 7:3 (70/30) via `grid-cols-10` + `col-span-7`/`col-span-3` (still non-arbitrary, per the same lint constraint as round 2) — a +5 percentage-point widening, at the low end of the user's requested 5-10% range. Detail panel's padding went from `p-card-padding` (20px) to `p-8` (32px) — both are named/numbered steps on the same design-system spacing scale (`packages/theme/src/spacing.css`'s `--m-space-*` tokens, exposed as Tailwind's numeric `p-*` utilities via `tailwind-preset.css`), not arbitrary values.
- turbo lint/build/test re-verified clean (94/94 tests) after round 3; live-verified via Playwright in dark mode against a busy synthetic photo (solid-color fixtures don't show letterboxing/padding clearly) — confirmed visible padding around the photo and a visibly wider, more padded detail panel, matching the user's reference screenshot. Zero console errors.
- **Round 4 (user feedback, 2026-07-08)**: three changes. (1) Detail-panel eyebrow `"// Photo"` → `"// EXIF"`. (2) Panel title (the formatted `capturedAt`, e.g. `"14 Jun 2026, 18:32"`) shrunk from `ModalHeader`'s default `text-h1` (32px) to `text-h3` (18px) — the design system's type scale has no intermediate size between the two, so this is the nearest token rather than an arbitrary value; added a `titleClassName` override prop to `ModalHeader` (`packages/ui`, additive/backward-compatible, mirroring the existing `eyebrowClassName` override pattern) rather than changing every `ModalHeader` consumer's default. (3) Spec-row order changed to Captured, Lens, Aperture, Shutter speed, ISO, Exposure comp., Megapixel mode, Camera, per the user's explicit sequence.
- **Also during round 4** (user request: "apply the same ordering everywhere — Facets panel, the modal, and Insights, so it's all consistent"): `FacetPanel.tsx` reordered to Year, Lens, Aperture, Shutter, ISO, Exposure comp, Megapixel mode, Camera (Camera moved from first to last). `aggregations.ts`' `computeInsights` reordered to hour, lens, aperture, shutter, iso, megapixel, camera. **Flagged to the user first**: Insights' original order (`lens, iso, shutter, aperture, megapixel, camera, hour`) was a formal Acceptance Criterion in `planning-artifacts/epics.md` for the already-`done` Story 2.4 ("it shows one histogram-bar row per FR-7 dimension — focal length/lens (combined), ISO, shutter, aperture, megapixel mix, selfie/rear, hour-of-day"), not just an internal convention — reordering it is a deliberate deviation from that story's accepted AC, not a bug fix. User confirmed reordering anyway for cross-page consistency; the order-assertion test in `aggregations.test.ts` was updated to match and documents the deviation inline.
- **Also during round 4** (user request: verify inter-section spacing consistency site-wide): audited `App.tsx`/`Browse.tsx`/`Insights.tsx` for spacing-token usage. Found `App.tsx`'s sidebar/content-column split already used `gap-10` (40px) — numerically identical to `--m-space-section-rhythm` (also 40px) but under the generic numbered token rather than the semantic name the user cares about — switched it to `gap-section-rhythm` (zero visual change, clarifies intent). Browse's and Insights' own eyebrow-to-content gaps (`mt-7`, 28px/item-gap) were already identical to each other pre-existing from Stories 3.2-3.4/2.4, so no change was needed there — flagged as already consistent rather than silently left alone.
- turbo lint/build/test re-verified clean (94/94 tests, `aggregations.test.ts`'s order assertion updated) after round 4; live-verified via Playwright — confirmed the `// EXIF` eyebrow, the smaller title, the new Spec-row order in the modal, the reordered `FacetPanel` (extracted the rendered facet labels' text order), and the reordered Insights dimensions (extracted the rendered tab's visible text). Zero console errors.
- **Round 5 (user bug reports, 2026-07-08)**: five fixes. (1) **Real bug in `packages/ui/Modal.tsx`**: a full-resolution portrait photo could render taller than the viewport with its top silently clipped off-screen and no way to scroll to it — root cause was the overlay's `flex items-center justify-center overflow-y-auto` combination, a well-known CSS issue where flexbox center-alignment clips an overflowing child at its *start* rather than letting it scroll into view. Fixed by switching to `grid place-items-center` (Grid's centering doesn't have this clipping behavior) — benefits every Modal consumer, not just this one. Additionally capped the `<img>` at `75vh` via inline `style` (same `tailwindcss/no-arbitrary-value` escape hatch as `HistogramBar.tsx`) so typical photos need no scrolling at all and always leave visible margin. (2) **Title now shows the actual filename, not the capture date** — the user's round-4 "photo's name" request was misread as the formatted `capturedAt`; added a new required `fileName: string` field to `Photo`/`WorkerPhoto` (always available from `File.name`, unlike the genuinely-optional EXIF-derived fields), threaded through `exif-worker.ts` exactly like `blob` already was (attached at the same call sites, kept out of `normalize.ts`'s pure functions), and `PhotoDetailModal`'s title now reads `photo.fileName` directly — no fallback needed since it's never missing. `Captured` (the date) stays as the first Spec row, now the sole place the date appears. (3) **Grid-cell hover feedback** — `PhotoGridCell` had zero hover/cursor feedback at all; added `cursor-pointer` plus a `group`/`group-hover:border-accent` (and `group-focus-visible:` for keyboard parity) accent border on the thumbnail, and a `focus-visible` outline on the button itself (a real, pre-existing a11y gap — no consumer of this button previously got any focus indicator). (4) **`HeaderBar` border too high-contrast** — it alone used `border-line` (`#161616` light / `#e6e6e6` dark, essentially foreground-level contrast) while every other bordered element in both apps uses the softer `border-dim`; changed to match (no other `border-line` consumers existed, so this is a pure fix, not a partial one). (5) **Facets `<aside>` border removed** per direct user request — `bg-panel` alone now sets it off from the page background.
- **Also during round 5** (user bug report, unrelated to the modal): the ingest-progress `StatBar` (`packages/ui`) rendered its raw `value` prop verbatim as the displayed/announced percentage (e.g. `"83.33333333333333%"`) — `filled` (the cell count) was already rounded but the text label wasn't. Added one `Math.round`, reused for both the display text and `aria-valuenow`. `StatBar` has exactly one consumer (`IngestProgress.tsx`), so this couldn't regress anything else.
- turbo lint/build/test re-verified clean (94/94 tests) after round 5; live-verified via Playwright with a new 3000×6000 synthetic portrait fixture — confirmed the dialog's bounding box now fits entirely within the viewport, the title renders the fixture's actual filename, the grid cell's computed `cursor` is `pointer` and its border computes to the accent color on hover, the header's border computes to the `dim` token's color, and the Facets `<aside>`'s computed border-width is `0px`. Zero console errors.
- **Round 6 (user UI request, 2026-07-08, touches Insights/Story 2.4's territory, not this story's own scope, but continued in this session as a direct extension of the round-4/5 cross-cutting polish work)**: two changes. (1) **Real bug, `HistogramBar.tsx`**: the bar's grid column used `1fr`, stretching that track to the full available row width regardless of how many cells actually render — the count then sat wherever that (potentially huge) track happened to end, far from the visible cell strip. Changed to `auto`/`auto` (both bar and count columns size to their own content) so the count always sits immediately after the bar, with unused row width left empty rather than stretched into the bar — matches the user's reference screenshot exactly. (2) **Insights gained a small companion chart per Panel** — every dimension keeps its own full-width `Panel`, stacked exactly as before (an initial version of this round wrongly regrouped dimensions into two separate panel groups instead of leaving each Panel where it was — corrected same-day per immediate user feedback, "you broke it — each panel stays as before, the bar-list on the left as it was, just a small chart a bit to the right in that *same* panel showing the same thing in a different form"). Each Panel's body is now `flex` two-column internally: the existing bar-list unchanged on the left (`flex-1`), a compact `Sparkline` (`packages/ui`, fixed `w-40` — "not very big" per the user — the "chart in the component library" they pointed at) on the right, for every one of the 7 dimensions. A trend line needs its series in left-to-right natural order, not `bucketRows`' descending-by-count order (that order is untouched — still what the bar-list rows and Story 2.4's original AC read from) — added `sortRowsForTrend(key, rows)` to `aggregations.ts`, unit-tested for all 6 ordered dimensions (fixed reference sequences for hour/shutter/iso/megapixel, whose bucket labels are pre-binned ranges or fixed enums rather than raw values; numeric parsing for lens/aperture, whose labels are exact values); `camera` passes through unchanged (only 2 unordered categories).
- turbo lint/build/test re-verified clean (101/101 tests, 7 new for `sortRowsForTrend`) after round 6's correction; live-verified via Playwright with 6 synthetic photos spanning distinct lens/aperture/shutter/iso/hour buckets, full-page desktop screenshot (1560×1300) — confirmed every one of the 7 dimensions kept its own single stacked Panel with the tightened bar/count spacing on the left and a small Sparkline trend on the right. Zero console errors.
- **Round 7 (user reference screenshot, 2026-07-08)**: refined round 6's per-Panel split further — the bar-list/chart columns changed from "bar-list `flex-1`, chart fixed `w-40`" to an exact `grid-cols-2` 50/50 split, matching the user's reference screenshot's proportions precisely ("занимает левую колонку в 50 процентов... справа чарт, который тоже занимает 50 процентов" — "takes the left column at 50 percent... the chart on the right also takes 50 percent"). Also added a bucket-label row directly under each Sparkline (`flex justify-between`, one label per point, in the same ascending order the chart itself uses) — `Sparkline` renders no axis labels of its own, so without this the chart's x-axis was unreadable; the reference screenshot explicitly called this out ("снизу надо показывать значения чтоб было очевиднее" — "show the values underneath so it's more obvious").
- turbo lint/build/test re-verified clean (101/101 tests, no test changes needed — purely a layout/labeling change to already-tested data) after round 7; live-verified via Playwright in dark mode (matching the reference screenshot's theme) with the same 6-photo batch — confirmed the 50/50 column split and each chart's label row rendering the correct bucket sequence beneath it for all 7 dimensions. Zero console errors.
- **Round 8 (user reference screenshots, 2026-07-08)**: five changes, all from a single message pointing at two overflow/legibility problems. (1) **Capped every dimension to its top 10 buckets by share** — a dimension with many distinct values (the reference screenshot showed 15 distinct focal lengths) overflowed both the bar-list and, worse, the Sparkline's label row spilled outside the Panel's right edge entirely. Added `topRowsByShare(rows, limit = 10)` to `aggregations.ts`, unit-tested — a plain truncation since `bucketRows`' rows are already sorted descending by count, so this keeps the most significant buckets, applied identically to the bar-list and (before natural-sorting) the trend chart so both show the same subset. (2) **Removed the `hour` ("Hour of Day") dimension entirely** — explicit user request ("вообще лучше убрать хоур оф дэй отсюда, вообще убери его"), not a display tweak: `bucketHour` and its `computeInsights` entry were deleted outright, along with its now-dead `sortRowsForTrend` branch and its tests; Insights now shows 6 dimensions, not 7. (3) **Megapixel and camera lost their Sparkline** — only `lens`/`aperture`/`shutter`/`iso` (`TREND_DIMENSION_KEYS`) get the 50/50 chart split now; the other two render as a bar-list alone at full Panel width instead of sharing a 50/50 grid with a chart column reduced to a single meaningless dot (only 2 categories each, not a meaningful trend shape) — this also fixes the second reference screenshot's `"71 · 100%"` text-wrapping, since the bar-list gets the full Panel width back instead of being squeezed into a 50%-width column with nothing useful on the other side. (4) **A dimension with zero rows now renders no Panel at all**, instead of an empty "No data for this batch." Panel — explicit user request. (5) **`HistogramBar`'s cell count reduced from its 48 default to 32 within Insights** (via the existing `cells` prop, not a change to the component's own default) — extra headroom for the count/percentage text now that trend dimensions are permanently half-width, per the user's explicit "the bar chart maybe reduce its width a bit" note about large numbers not fitting.
- turbo lint/build/test re-verified clean (101/101 tests: 7 aggregations tests removed for the dropped `hour` dimension/branch, 3 new for `topRowsByShare`) after round 8; live-verified via Playwright with 71 synthetic photos across 15 distinct focal lengths (matching the reference screenshot's scale) — confirmed exactly 10 Focal-length rows render (not 15), its label row fits on one line inside the Panel with no overflow, no Hour-of-day Panel exists anywhere in the output, Megapixel/Camera render as a single full-width bar-list with no orphaned chart column, and `"71 · 100%"` renders on one line without wrapping. Zero console errors.
- **Round 9 (user request, 2026-07-08)**: two changes. (1) **ISO and shutter now group by exact value, not a pre-binned range/stop** — explicit user request ("не применяем... группировку... чтоб не было исо 400-1600, а по одиночному значению, как для апертуры[,] шатера"): `bucketIso`'s 4 fixed ranges and `bucketShutter`'s `SHUTTER_STOPS` standard-stop table were deleted outright (along with `ISO_BUCKET_ORDER`/`SHUTTER_LABEL_ORDER`, the fixed sequences `sortRowsForTrend` indexed into for their natural order), matching `aperture`'s pre-existing exact-value behavior (`f/1.8`, not a range). ISO's bucket key is now `` `ISO ${iso}` `` (matching the "ISO 200" convention used everywhere else in this app, e.g. the grid badge/Spec-row); shutter's is a new local `formatShutterLabel` — a third copy of the same one-line formatting formula already duplicated in `FacetPanel.tsx` and `PhotoDetailModal.tsx`, not imported from either since `insights` importing from `browse` is blocked by the AD-3 eslint boundary rule and reaching into `photo-detail` for it would be an odd new coupling. `sortRowsForTrend`'s natural order for both now parses the exact numeric value straight out of the label (a small regex for the `1/Ns` fraction case) instead of indexing a fixed sequence. (2) **Removed the `camera` ("Front / Rear") dimension entirely** — explicit user request, same treatment as round 8's `hour` removal: deleted from `computeInsights` outright, not just hidden; Insights now shows 5 dimensions.
- turbo lint/build/test re-verified clean (99/99 tests: iso/shutter bucket-range and camera tests replaced with exact-value equivalents) after round 9; live-verified via Playwright with 6 synthetic photos across 6 distinct ISO/shutter values each — confirmed every ISO row reads `"ISO {n}"` and every shutter row its exact value (e.g. `"1/2000s"`), both sorted correctly in the trend chart's label row, and no Front/Rear panel renders anywhere. Zero console errors.
- **Round 10 (user reference screenshot + correction, 2026-07-08)**: round 8's top-10-by-share cap still overflowed a busy dimension's chart-label row (the reference screenshot showed 10 focal lengths still spilling past the Panel edge) — lowered `topRowsByShare`'s default `limit` from 10 to 5. The user then corrected scope mid-implementation, before this was verified: the cap was originally applied to *both* the bar-list and the chart (same as round 8); the user clarified it should apply to the chart side only — "только для правого чарта мы ограничиваем, а слева где бары там всю статистику показываем" ("only the right chart gets limited; on the left where the bars are, show all the statistics"). `DimensionPanel` (`Insights.tsx`) now renders the bar-list from `dimension.rows` directly (every bucket, uncapped) and only passes `topRowsByShare(dimension.rows)` into `sortRowsForTrend` for the Sparkline/label row.
- turbo lint/build/test re-verified clean (99/99 tests, `topRowsByShare`'s default-limit test updated from 10→5 rows) after round 10; live-verified via Playwright with the same 71-photo/15-focal-length batch — confirmed the bar-list renders all 15 rows while the chart's label row shows only the top 5 (`20mm, 25mm, 26mm, 29mm, 32mm`). Zero console errors.
- **Round 10 continued (user request + correction, 2026-07-08)**: added a `"// Top {MAX_ROWS_SHOWN}"` eyebrow next to the chart so the truncation is visible rather than silent (`MAX_ROWS_SHOWN` exported from `aggregations.ts` so the label can never drift from the actual cap value). First attempt placed it inside the chart's own column with the row's `items-center` changed to `items-start`, which only lined the new eyebrow up with the bar-list's *first row* — one line below the dimension's own caption above it. User caught this precisely ("это не на одном уровне" — "that's not on the same level") with a cropped screenshot showing the two eyebrows on different lines. Fixed properly: widened `Panel`'s `caption` prop from `string` to `ReactNode` (`packages/ui`, backward-compatible — every existing string caption still renders identically) so `Insights.tsx` can pass a `grid-cols-2` two-eyebrow header (dimension caption + "Top 5") into `Panel`'s own caption slot, guaranteeing both sit on the exact same line by construction rather than by matching unrelated layout heights.
- turbo lint/build/test re-verified clean (99/99 tests) after this fix, across `@bmad/gallery` and `@bmad/ui`; live-verified via Playwright — confirmed `// FOCAL LENGTH` and `// TOP 5` render on the same pixel row.
- **Round 11 (user reports, real phone EXIF data, 2026-07-08)**: two unrelated fixes. (1) **Real bug — unrounded aperture/exposure-comp floats**: real phone EXIF is "a bit shaky" (user's words) — `apertureF`/`exposureCompEv` compute as imprecise floats (e.g. `1.7799999713880652`, `0.33539035466185535`) instead of the clean `f/1.8`/`+0.3` a camera actually reports, spilling ugly long decimals into the Facets sidebar (slider range labels, exposure-comp checkboxes), the grid badge, the Photo-detail-modal's Spec row, and Insights' Aperture dimension. Fixed by rounding to 1 decimal at every one of the (now four, per the established per-feature-boundary duplication pattern) independent formatting sites: `FacetPanel.tsx`'s `formatAperture`/`formatExposureComp` (both newly exported for testing), `PhotoGridCell.tsx`'s `formatExifBadgeSegments` (shared by the grid badge and, via reuse, the modal's Spec row), and `aggregations.ts`'s inline aperture bucket key — the last one matters for more than display: without rounding, two real photos at "the same" f/1.8 but with slightly different floating-point noise would've landed in two separate spurious buckets instead of grouping correctly. (2) **Insights' trend chart redesigned again, per direct user request superseding rounds 8/10/10-continued**: removed the `"// Top 5"` eyebrow and the always-visible bucket-label row entirely; the chart now plots *every* bucket (no cap) and shows a bucket's label as a native browser tooltip on hovering its point instead — added an optional `labels?: string[]` prop to `Sparkline` (`packages/ui`, parallel to `series`, rendered as each dot's `title` attribute). Since there's no longer a label row to overflow, the cap that motivated it is gone too: `topRowsByShare`/`MAX_ROWS_SHOWN` removed outright from `aggregations.ts` (unused anywhere afterward) along with their tests, and the chart/bar-list grid reverted from `items-start` back to `items-center` (recentered "like before") now that there's no second eyebrow needing alignment — `Panel`'s `caption` prop (`packages/ui`) reverted from the round-10 `ReactNode` widening back to plain `string`, since the two-eyebrow header that required it no longer exists.
- turbo lint/build/test re-verified clean (101/101 tests: `topRowsByShare` tests removed, new rounding tests added for `formatAperture`/`formatExposureComp`/`formatExifBadgeSegments`/aggregations' aperture grouping) after round 11, across `@bmad/gallery` and `@bmad/ui`; live-verified via Playwright with a photo carrying deliberately-irrational EXIF rationals (`FNumber: 16/9`, `ExposureBiasValue: -2/3`) — confirmed the grid badge, Facets sidebar (slider range + EV checkboxes), and Insights' Aperture dimension all show clean rounded values (`f/1.8`, `-0.7`), the chart plots all 15 focal-length buckets (not 5), and a dot's `title` attribute carries its bucket label. Zero console errors.
- **Round 12 (user reference screenshots, 2026-07-08)**: `bucketRows` (`aggregations.ts`) previously broke same-count ties with no defined order — a run of rows all tied at "1 photo" fell back to `Map` insertion order (whatever order the underlying photos happened to appear in), which reads as arbitrary ("ISO 640, 20, 40, 64, 32, 16, 1000, 400 …" per the user's screenshot). Added a same-count tiebreaker: ascending by natural value. Extracted a shared `naturalValue(label)` helper — dispatching on each label format's own distinguishing prefix/suffix (`"ISO "`, `"f/"`, `"mm"`, `"MP"`, trailing `"s"`) — reused by both `bucketRows`' new tiebreaker and `sortRowsForTrend` (refactored to a single generic sort using it, removing the 4 near-duplicate per-key branches that previously each re-implemented the same parsing).
- turbo lint/build/test re-verified clean (105/105 tests, 1 new test for the tiebreaker) after round 12; live-verified via the existing Insights Playwright checks (unaffected dimensions still pass) plus the new unit test covering the exact ISO sequence from the user's screenshot.
- **Round 13 (user screenshot, 2026-07-08)**: the Facets sidebar's Exposure-comp checkbox list showed two visually-identical `"+0.3"` entries — a direct consequence of round 11's *display-only* rounding: `computeFacetValueOptions`'s distinct-value dedup (`ingestStore.ts`, a plain `Set` over the raw floats) still saw two different near-identical raw `exposureCompEv` values (e.g. `0.33539035466185535` vs `0.3353903590644559`) as genuinely distinct, so it built two options that both happened to *round* to the same display label. Rounding only the label could never fix this — the underlying values themselves needed to match. Moved the fix to its correct root: `normalize.ts` now rounds `apertureF`/`exposureCompEv` to 1 decimal on the **stored** `Photo` value itself (new exported `roundToOneDecimal`, unit-tested), at EXIF-normalization time, not just at each display site. This makes two real photos at "the same" f-stop/EV carry identical numbers everywhere downstream — `computeFacetValueOptions`'s `Set`-based dedup and `matchesDiscrete`'s exact-equality filter matching (`ingestStore.ts`) both correctly treat them as one value, not two, with no separate epsilon-tolerance logic needed. The four round-11 display-site roundings were left in place (rounding an already-rounded value is a harmless no-op) as a defensive second layer rather than removed, to avoid unnecessary churn on already-tested code.
- turbo lint/build/test re-verified clean (105/105 tests, 3 new for `roundToOneDecimal`) after round 13; live-verified via Playwright with two photos carrying different odd EXIF rationals for the same real EV (`1/3` and `5/15`, both `0.333…`) plus a third at a genuinely different EV (`2/3`) — confirmed the checkbox list shows exactly 2 entries (`+0.3`, `+0.7`), not 3, and that checking the deduped `+0.3` box correctly matches both of the near-duplicate source photos (not just one). Zero console errors.
- **Round 14 (user reports, 2026-07-08)**: two fixes. (1) **`Sparkline`'s hover label wasn't reading as working** — round-11's native `title`-attribute tooltip depends on the browser's own hover delay/styling, which the user experienced as "nothing happens." Replaced with a small custom tooltip: `Sparkline` (`packages/ui`) gained `useState` tracking a hovered point index, showing that point's `labels` entry in a bordered `bg-bg` box positioned above the dot, appearing immediately on `onMouseEnter` rather than after the browser's native delay. Also widened each point's actual hoverable hit area from the visible `size-1.5` (6px) dot to an invisible `size-4` (16px) target centered on it, since the visible dot alone was a small, fiddly mouse target. (2) **Real layout bug — non-trend Panels didn't actually expand to use their full width**: `HistogramBar`'s bar column is `auto`-sized to its fixed cell count, not stretched to fill available space (round-6's own fix for a different overflow problem) — so Megapixel Mix's bar rendered at the exact same pixel width as the trend dimensions' bars above it, even though its Panel has no chart taking up the other half and had the room to be much wider. Split the single `BAR_CELLS` constant into `BAR_CELLS_TREND` (32, unchanged) and `BAR_CELLS_FULL_WIDTH` (64) in `Insights.tsx`, chosen per-Panel by whether the dimension gets a trend chart.
- turbo lint/build/test re-verified clean (105/105 tests, no test changes needed — both were rendering/layout-only fixes to already-tested data) after round 14, across `@bmad/gallery` and `@bmad/ui`; live-verified via Playwright — confirmed hovering a Focal-length point immediately shows a `"12mm"`-style tooltip box (no native-tooltip delay), and a full-page screenshot confirms Megapixel Mix's bar now renders visibly wider than the trend dimensions' bars above it.
- **Round 15 (user correction, 2026-07-08)**: round 14's Megapixel-Mix fix was wrong in direction — widening the bar to 64 cells made it overshoot the same column boundary the charted Panels establish, rather than matching it. User clarified with an annotated screenshot: "I meant it stays one column at 50 percent, so everything's consistent" — i.e. Megapixel Mix's bar-list should sit in the *same* left `grid-cols-2` column as the charted Panels (same `BAR_CELLS`, same column width), just with the right column simply empty, so every Panel's label/bar/count boundaries line up down the whole page. Collapsed `BAR_CELLS_TREND`/`BAR_CELLS_FULL_WIDTH` back into a single `BAR_CELLS` (32), and `DimensionPanel` now always renders the `grid-cols-2` wrapper — the `Sparkline` cell is simply omitted (not the whole grid) for non-trend dimensions.
- turbo lint/build/test re-verified clean (105/105 tests) after round 15; live-verified via Playwright full-page screenshot — confirmed Megapixel Mix's bar/count columns now line up exactly with the trend dimensions' columns above it.
- **Rounds 16–21 were retroactively moved out of this story's Dev Agent Record (2026-07-08)**: these rounds happened in the same dev-story session but touched territory outside "Photo Detail Modal" — HEIC support/thumbnail-performance/progressive-per-photo-loading (rounds 16–18, 20) are Story 2.2's own ingest/worker pipeline; the Exposure-comp-to-slider conversion and its step-dot tick color fix (rounds 19, 21) are Story 3.3's own Facet-panel. Full write-ups now live in **2-2-in-browser-metadata-extraction-web-worker.md**'s "Post-completion fix-up round 1–4" and **3-3-facet-panel-live-filtering.md**'s "UX designer fix-up round 9–10" respectively. This story's own File List/Change Log below have been trimmed to match — `PhotoDetailModal.tsx`'s round-16 entry is kept, since switching its `<img>` to the new `fullUrl` field is genuinely this story's own concern even though the field itself was introduced in Story 2.2.

### File List

- `apps/gallery/src/features/photo-detail/Spec.tsx` (new; round 2 — restyled to a stacked label/value row)
- `apps/gallery/src/features/photo-detail/PhotoDetailModal.tsx` (new; round 2 — rebuilt as a 3:1 photo/detail-panel grid using `Modal`'s new `variant="wide"`, now renders the actual photo; round 3 — widened to 7:3, added `p-section-rhythm` photo padding and `p-8` panel padding; round 4 — `// EXIF` eyebrow, smaller `text-h3` title, reordered Spec rows; round 5 — title now `photo.fileName`, `<img>` capped at `75vh`; round 16 — `<img>` now reads `photo.fullUrl` (full-resolution, the field itself introduced in Story 2.2), not `thumbnailUrl` (now genuinely small))
- `apps/gallery/src/features/photo-detail/PhotoDetailModal.test.ts` (new — 17 tests; round 4 — updated the `formatSpecRows` order assertion; round 5 — fixture helper gained `fileName`; round 16 — fixture helper gained `fullUrl`)
- `apps/gallery/src/features/browse/Browse.tsx` (modified — `selectedPhoto` state, wires `PhotoGridCell.onOpen`, renders `PhotoDetailModal`)
- `apps/gallery/src/features/browse/PhotoGridCell.tsx` (modified — exported `MISSING_FIELD_PLACEHOLDER`, updated stale doc comments now that `onOpen` is wired; round 5 — `cursor-pointer`, `group-hover`/`group-focus-visible` accent border, `focus-visible` outline; round 11 — `formatExifBadgeSegments` rounds aperture to 1 decimal; see Story 2.2 for its later placeholder-rendering change)
- `apps/gallery/src/features/browse/PhotoGridCell.test.ts` (modified; round 5 — fixture helper gained `fileName`; round 11 — new test for the aperture-rounding fix)
- `apps/gallery/src/features/browse/FacetPanel.tsx` (modified; round 4 — reordered Facets to Year/Lens/Aperture/Shutter/ISO/Exposure comp/Megapixel/Camera; round 11 — `formatAperture`/`formatExposureComp` round to 1 decimal, `formatAperture` newly exported for testing; see Story 3.3 for its later Exposure-comp-slider change)
- `apps/gallery/src/features/browse/FacetPanel.test.ts` (modified; round 11 — new rounding tests for `formatAperture`/`formatExposureComp`)
- `apps/gallery/src/features/insights/aggregations.ts` (modified; round 4 — reordered `computeInsights`' dimensions, deliberately deviating from Story 2.4's original AC #2 order, confirmed with the user; round 6 — added `sortRowsForTrend`, covering all 6 ordered dimensions; round 8 — removed `bucketHour`/the `hour` dimension entirely and its now-dead `sortRowsForTrend` branch; added `topRowsByShare`; round 9 — removed `bucketIso`/`bucketShutter`'s range/stop bucketing and the `camera` dimension entirely, both in favor of exact-value grouping; round 10 — `topRowsByShare`'s default limit lowered 10→5, `MAX_ROWS_SHOWN` exported for the chart's eyebrow label; round 11 — aperture bucket key rounds to 1 decimal (fixes both display and spurious duplicate-bucket grouping); `topRowsByShare`/`MAX_ROWS_SHOWN` removed outright, now unused anywhere; round 12 — `bucketRows` gained a same-count tiebreaker (ascending natural value); extracted the shared `naturalValue` helper, `sortRowsForTrend` refactored to a single generic sort using it)
- `apps/gallery/src/features/insights/aggregations.test.ts` (modified; round 4 — updated the dimension-order assertion to match, documents the deviation; round 5 — fixture helper gained `fileName`; round 6 — 7 new tests for `sortRowsForTrend`; round 8 — removed the `hour`-specific tests, added 3 new tests for `topRowsByShare`; round 9 — iso/shutter bucket-range tests and the `camera` dimension test replaced with exact-value equivalents; round 10 — `topRowsByShare`'s default-limit test updated to expect 5 rows, not 10; round 11 — `topRowsByShare` tests removed, new test for aperture-rounding/grouping; round 12 — new test for the same-count tiebreaker)
- `apps/gallery/src/features/insights/Insights.tsx` (modified; round 6 — every Panel's body is now two internal columns: the unchanged bar-list plus a `Sparkline` alongside it, for all 7 dimensions; single stacked Panel list unchanged; round 7 — exact `grid-cols-2` 50/50 split, added a bucket-label row under each Sparkline; round 8 — rows capped via `topRowsByShare`, only `lens`/`aperture`/`shutter`/`iso` keep the Sparkline split, dimensions with zero rows render no Panel, `HistogramBar` cell count reduced to 32; round 10 — `topRowsByShare` now applies to the chart's rows only, the bar-list always renders every bucket uncapped; added a `"// Top {MAX_ROWS_SHOWN}"` eyebrow passed into `Panel`'s widened `caption` slot as a two-column header, guaranteeing it lines up with the dimension's own caption; round 11 — removed the eyebrow and the label row entirely, chart plots every bucket via `Sparkline`'s new `labels` prop (native hover tooltip) instead, grid reverted to `items-center`, plain string `caption` again; round 14 — split `BAR_CELLS` into `BAR_CELLS_TREND`(32)/`BAR_CELLS_FULL_WIDTH`(64) so non-trend Panels (megapixel) actually use their full width instead of matching the trend Panels' half-width bar size; round 15 — reverted round 14's split back to a single `BAR_CELLS`(32) per user correction — non-trend Panels stay in the *same* `grid-cols-2` column as trend Panels (empty right cell), not a wider bar)
- `apps/gallery/src/features/insights/HistogramBar.tsx` (modified; round 6 — fixed the bar/count spacing bug, `1fr` → `auto` grid column)
- `packages/ui/src/Panel/Panel.tsx` (modified; round 10 — widened `caption` prop from `string` to `ReactNode`; round 11 — reverted back to `string`, the two-eyebrow header that needed the widening no longer exists)
- `packages/ui/src/Sparkline/Sparkline.tsx` (modified; round 11 — added an optional `labels?: string[]` prop, initially rendered as each point's `title` attribute for a native hover tooltip; round 14 — replaced the native-tooltip approach with a custom `useState`-driven tooltip shown immediately on hover, plus a larger invisible hit target around each point's dot)
- `apps/gallery/src/store/ingestStore.test.ts` (modified; round 5 — fixture helper gained `fileName`; see Story 2.2/3.3 for later changes)
- `apps/gallery/src/worker/types.ts` (modified; round 5 — added required `fileName: string` to `Photo`; see Story 2.2 for its later `fullUrl`/`WorkerMessage` changes)
- `apps/gallery/src/worker/normalize.ts` (modified; round 5 — `normalizeExifFields`/`createUnreadablePhoto` return types now exclude `fileName` too, mirroring the existing `blob` exclusion; round 13 — added `roundToOneDecimal`, applied to `apertureF`/`exposureCompEv` at normalization time, the real fix for the Facet-panel near-duplicate dedup bug)
- `apps/gallery/src/worker/normalize.test.ts` (modified; round 13 — 3 new tests for `roundToOneDecimal`)
- `apps/gallery/src/worker/exif-worker.ts` (modified; round 5 — attaches `fileName: file.name` at both `parseFile` return sites, alongside `blob`; see Story 2.2 for its later message-protocol change)
- `packages/ui/src/Modal/Modal.tsx` (modified — fixed backdrop-click-to-close [round 1]; added `variant` prop [round 2]; round 5 — overlay centering `flex` → `grid place-items-center`, fixing a real content-clipping bug)
- `packages/ui/src/index.ts` (modified — exported the new `ModalVariant` type)
- `packages/ui/src/Modal/ModalHeader.tsx` (modified; round 4 — added a `titleClassName` override prop, default unchanged)
- `packages/ui/src/HeaderBar/HeaderBar.tsx` (modified; round 5 — `border-line` → `border-dim`)
- `packages/ui/src/StatBar/StatBar.tsx` (modified; round 5 — rounds the displayed/announced percentage)
- `apps/gallery/src/app-shell/App.tsx` (modified; round 2 — `self-start` on the Facets `<aside>`; round 4 — `gap-10` → `gap-section-rhythm`, same value, clearer semantics; round 5 — removed the Facets `<aside>`'s border)

## Change Log

- 2026-07-08: Implemented Story 3.5 — `Spec` label/value row component, `PhotoDetailModal` (8 formatted metadata rows atop Story 1.5's Modal/ModalHeader shell), wired `PhotoGridCell.onOpen` in Browse.tsx to open it. Found and fixed a pre-existing backdrop-click-to-close bug in `packages/ui/Modal.tsx` (uncovered by this story's live verification, affects every Modal consumer). turbo lint/build/test clean (94/94 tests, 17 new); live-verified via Playwright against synthetic Pillow-generated EXIF fixtures.
- 2026-07-08: Round-2 redesign (user feedback) — `Modal` gained a `variant="wide"` option; `PhotoDetailModal` rebuilt as a 3:1 photo/metadata grid (photo now actually renders in the modal, previously text-only), collapsing to a stacked layout below `lg`; `Spec` restyled label-over-value. Also fixed an unrelated user-reported layout bug: the Facets `<aside>` in `App.tsx` no longer stretches to the tab content column's height (`self-start`). turbo lint/build/test clean across gallery/ui/landing; live-verified via Playwright at desktop and mobile viewports plus a full-page screenshot of the sidebar fix.
- 2026-07-08: Round-3 spacing pass (user feedback) — photo column gained `p-section-rhythm`, the 3:1 split widened to 7:3, detail panel padding increased from `p-card-padding` to `p-8`; all three values pulled from the existing design-system spacing scale. turbo lint/build/test clean (94/94 tests); live-verified via Playwright in dark mode with a busy synthetic photo.
- 2026-07-08: Round-4 pass (user feedback) — modal eyebrow → `// EXIF`, title shrunk to `text-h3` (new `ModalHeader.titleClassName` override prop), Spec rows reordered to Captured/Lens/Aperture/Shutter/ISO/Exposure comp/Megapixel/Camera; the same relative order applied to `FacetPanel` and (after flagging + user confirmation, since it deviates from Story 2.4's original AC #2) `Insights`' dimension list, for full cross-page consistency. Also normalized `App.tsx`'s sidebar/content gap to the named `gap-section-rhythm` token (same 40px value). turbo lint/build/test clean (94/94 tests); live-verified via Playwright across Browse, the modal, and Insights.
- 2026-07-08: Round-5 bug-fix pass (user reports) — fixed a real Modal content-clipping bug (`flex items-center` → `grid place-items-center` in `packages/ui/Modal.tsx`) plus a `75vh` cap on the detail photo so oversized photos never exceed the viewport; modal title now shows the photo's actual filename (new required `Photo.fileName` field, threaded through the ingest worker) instead of the capture date; `PhotoGridCell` gained hover/focus feedback (`cursor-pointer`, accent border, focus-visible outline) where previously there was none; `HeaderBar`'s overly high-contrast `border-line` changed to the standard `border-dim`; Facets `<aside>` border removed. Also fixed an unrelated reported bug: `StatBar` (packages/ui) now rounds its displayed/announced percentage instead of showing raw floating-point values like "83.33333333333333%". turbo lint/build/test clean (94/94 tests) across gallery/ui/landing; live-verified via Playwright with a new tall-portrait synthetic fixture plus computed-style assertions for every fix.
- 2026-07-08: Round-6 pass (user request, Insights) — fixed a real `HistogramBar` spacing bug (`1fr` → `auto` grid column, so the count sits right next to the bar instead of far away); added a small `Sparkline` trend chart alongside each dimension's existing bar-list (new `sortRowsForTrend` in `aggregations.ts` for the natural left-to-right order a trend needs). An initial version of this round wrongly split Insights into two separate panel groups instead of keeping every dimension's own single Panel — corrected same-day per immediate user feedback. turbo lint/build/test clean (101/101 tests, 7 new); live-verified via Playwright with 6 synthetic photos across distinct EXIF buckets.
- 2026-07-08: Round-7 pass (user reference screenshot) — the bar-list/Sparkline split in each Insights Panel is now an exact `grid-cols-2` 50/50, and each Sparkline gained a bucket-label row underneath it so its x-axis reads unambiguously (it renders no labels of its own). turbo lint/build/test clean (101/101 tests); live-verified via Playwright in dark mode.
- 2026-07-08: Round-8 pass (user reference screenshots) — every dimension capped to its top 10 buckets by share (new `topRowsByShare`, fixes both bar-list and chart-label overflow for high-cardinality dimensions); `hour` dimension removed entirely; megapixel/camera lost their Sparkline (render full-width bar-list instead of a half-width column next to a meaningless single-dot chart, also fixing a text-wrapping bug); dimensions with zero rows render no Panel at all; `HistogramBar` cell count reduced to 32 for more room around larger count/percentage text. turbo lint/build/test clean (101/101 tests); live-verified via Playwright with 71 photos across 15 focal lengths.
- 2026-07-08: Round-9 pass (user request) — ISO and shutter now group by exact value (`"ISO 200"`, `"1/500s"`) instead of a pre-binned range/stop table, matching aperture's existing exact-value behavior; `camera` ("Front/Rear") dimension removed entirely, same treatment as round 8's `hour` removal. turbo lint/build/test clean (99/99 tests); live-verified via Playwright.
- 2026-07-08: Round-10 pass (user reference screenshot + correction) — `topRowsByShare`'s cap lowered from 10 to 5 (10 still overflowed a busy dimension's chart labels), then rescoped mid-round per user correction to apply to the chart only — the bar-list now always shows every bucket uncapped; only the Sparkline/label row is capped to the top 5. turbo lint/build/test clean (99/99 tests); live-verified via Playwright with 71 photos across 15 focal lengths.
- 2026-07-08: Round-10 continued — added a `"// Top 5"` eyebrow next to the chart so the cap is visible, not silent. First attempt only lined it up with the bar-list's first row, not the dimension's own caption above — user caught the misalignment with a screenshot. Fixed by widening `Panel`'s `caption` prop (`packages/ui`) from `string` to `ReactNode` so `Insights.tsx` can pass both eyebrows as one `grid-cols-2` header, guaranteeing they sit on the same line. turbo lint/build/test clean (99/99 tests) across gallery/ui; live-verified via Playwright.
- 2026-07-08: Round-11 pass — fixed a real bug: real phone EXIF is imprecise (`f/1.7799999713880652`, `+0.33539035466185535`), so aperture/exposure-comp are now rounded to 1 decimal everywhere they're formatted (`FacetPanel.tsx`, `PhotoGridCell.tsx`'s shared badge formatter, `aggregations.ts`'s aperture bucket key — the last one also fixes real photos at "the same" f-stop spuriously splitting into separate buckets from floating-point noise). Also reworked the Insights trend chart per direct user request: removed the `"// Top 5"` eyebrow and the always-visible label row, the chart now plots every bucket and shows a point's label as a native hover tooltip instead (`Sparkline` gained an optional `labels` prop); `topRowsByShare`/`MAX_ROWS_SHOWN` removed as now-dead code, `Panel.caption` reverted to plain `string`. turbo lint/build/test clean (101/101 tests) across gallery/ui; live-verified via Playwright with deliberately-irrational EXIF rationals.
- 2026-07-08: Round-12 pass (user screenshots) — same-count rows in every bar-list previously fell back to arbitrary `Map` insertion order; `bucketRows` now breaks ties by ascending natural value via a new shared `naturalValue` helper, also used to simplify `sortRowsForTrend` into one generic sort. turbo lint/build/test clean (105/105 tests, 1 new).
- 2026-07-08: Round-13 pass (user screenshot) — the real fix for the Exposure-comp checkbox duplicate-label bug round 11 only patched at the display layer: `normalize.ts` now rounds `apertureF`/`exposureCompEv` on the stored `Photo` value itself (new `roundToOneDecimal`), so the Facet-panel's exact-equality dedup and filter matching both see identical numbers for "the same" real f-stop/EV instead of two near-identical floats. turbo lint/build/test clean (105/105 tests, 3 new); live-verified — deduped checkbox list, and filtering by it correctly matches both source photos.
- 2026-07-08: Round-14 pass (user reports) — `Sparkline`'s hover tooltip felt broken (native `title`'s browser-controlled delay/styling); replaced with a custom `useState`-driven tooltip appearing immediately on hover, plus a larger invisible hit target per point. Also fixed a real layout bug: non-trend Insights Panels (megapixel) didn't expand to use their full width, rendering the same fixed bar size as the half-width trend Panels above — split `BAR_CELLS` into `BAR_CELLS_TREND`/`BAR_CELLS_FULL_WIDTH` in `Insights.tsx`. turbo lint/build/test clean (105/105 tests) across gallery/ui; live-verified via Playwright.
- 2026-07-08: Round-15 pass (user correction) — round 14's Megapixel-Mix fix widened the bar in the wrong direction; user clarified it should stay in the *same* `grid-cols-2` column as the charted Panels (empty right cell), not a wider bar, for consistent alignment down the whole page. Collapsed back to a single `BAR_CELLS`. turbo lint/build/test clean (105/105 tests); live-verified — columns now line up exactly.
- 2026-07-08: Rounds 16–21, originally logged here, were retroactively moved to their real owning stories once the scope mismatch was noticed (see this story's Debug Log References for the full explanation): HEIC support/thumbnail-performance/progressive-loading work → **2-2-in-browser-metadata-extraction-web-worker.md** ("Post-completion fix-up round 1–4"); the Exposure-comp-slider conversion and its step-dot color fix → **3-3-facet-panel-live-filtering.md** ("UX designer fix-up round 9–10").
- 2026-07-08: Code review (3-layer adversarial: Blind Hunter, Edge Case Hunter, Acceptance Auditor) against the uncommitted diff — 1 decision resolved by user (story-attribution/documentation-consistency gap in the retroactive scope cleanup; accepted as-is, no backfill), 7 patches applied (stale-modal-photo bug fixed by deriving `Browse.tsx`'s selected photo live by id instead of a captured snapshot; `AddMoreControl`'s re-entrancy guard fixed with a new `parsing` store flag distinct from the progressive-commit `complete` flag; shutter-speed division-by-zero guarded in all 3 duplicated formatters; detail-modal photo `alt=""` → `alt={photo.fileName}`; zero-image-file folder/file picks now show a message; a stale doc comment fixed; Insights gained a "No data for this batch." fallback for the all-zero-dimensions case), 4 deferred (`heic2any` pre-1.0 dependency risk, worker/main-thread ordering fragility, progressive-commit store-churn cost, unverified `@bmad/landing` consumer of this session's `packages/ui` churn — all logged to `deferred-work.md`), 7 dismissed as noise (verified false positives, e.g. `formatCapturedAt`'s "no validation" claim against `normalize.ts`'s upstream regex guarantee, `StepTicks`' "unbounded DOM growth" claim against its actual index-bounded call sites). Also fixed a live user-reported bug found during this same session: `SingleSlider`'s track showed a meaningless accent fill from the left edge to the thumb; removed, leaving a neutral track with only the thumb accented. turbo lint/build/test clean (113/113 tests); live-verified via Playwright — confirmed 0 accent-fill bars in single-slider mode (vs. 1 in range mode) via screenshot, and the modal's `<img alt>` now carries the filename.
