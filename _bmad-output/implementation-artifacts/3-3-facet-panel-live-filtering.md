---
baseline_commit: 17b7007d3a2ff250f1a37ed10ebd86ed53c0e817
---

# Story 3.3: Facet Panel & Live Filtering

Status: review

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As the builder,
I want to filter my library by any facet,
so that I can narrow down to exactly the shots I'm looking for.

## Acceptance Criteria

1. **Given** the Browse tab, **when** the Facet-panel renders, **then** it appears as a sidebar on desktop (inside the 1240px container) or a slide-up sheet on mobile, with controls for lens, date-range, ISO, aperture, shutter, exposure comp, megapixel mode, and front/rear. [Source: planning-artifacts/epics.md#Story 3.3]
2. **Given** a discrete Facet (lens, megapixel mode, front/rear), **when** its control renders, **then** it uses Select/RadioGroup/Checkbox; for range Facets (date, ISO, aperture, shutter, exposure comp), it uses Range-control (two Fields, either side may be blank to mean unbounded). [Source: planning-artifacts/epics.md#Story 3.3]
3. **Given** a range Facet with min > max, **when** the invalid range is entered, **then** a FieldError shows on the offending side, and the filter doesn't apply until corrected. [Source: planning-artifacts/epics.md#Story 3.3]
4. **Given** any Facet control changes, **when** the change is committed, **then** the Browse grid re-filters immediately (AND-combined, no "Apply" button), the mobile trigger shows the active filter count, and "Clear-all" resets to the full readable set. [Source: planning-artifacts/epics.md#Story 3.3]
5. **Given** Facet filters are active on Browse, **when** Insights is viewed, **then** Insights remains unaffected (per Story 3.1). [Source: planning-artifacts/epics.md#Story 3.3]

## Dev Notes — read this first

- **Depends on Story 3.2** (a trivial `useFilteredPhotos()` alias — `() => useReadablePhotos()`, no filter state — and the sidebar layout region reserved but left empty). This story is what adds the `facetFilters` store slot, builds `useFacetFilters()` for the first time, and replaces `useFilteredPhotos()`'s trivial body with real filtering logic — the selector already exists and is already consumed by the grid, only its implementation changes here.
- **`Facet-panel` (UX-DR5) and `Range-control` (UX-DR6) are both Gallery-local**, not `packages/ui` — Browse is their only consumer (FR-2).
- **Control choice per discrete Facet — now confirmed by the real mockup (`mockups/gallery-browse.html`), correcting an earlier draft's guess:** `lens` → `Select`, **single-select** (the mockup shows a single current value, `"24mm ▾"`, not a multi-value chip list — an earlier draft of this story allowed `multiple` reasoning speculatively that multi-lens filtering might be wanted; the concrete mockup overrides that guess). `megapixelMode` → `RadioGroup` with options `All` (checked by default) / `12 MP (standard)` / `48 MP (ProRAW)` — matches the mockup's exact option labels. `camera` → `RadioGroup` with options `All` (default) / `Front (selfie)` / `Rear` — also matching the mockup's exact labels. The "All" option on both `RadioGroup`s is confirmed real (not an invented addition) — the mockup shows it as the checked default in both groups.
- **Facet fields use a collapsed/expandable summary pattern, per the mockup** — each Facet field shows a compact one-line trigger (`data-label` + current value + a `▾` chevron, e.g. `"Lens / focal length: 24mm ▾"`, `"ISO: All ISO ▾"`) that expands to reveal its real control (the `Select` options, or a `RangeControl`'s two `Field`s) on interaction. The mockup shows the date-range Facet in its **expanded** state (with visible FROM/TO `RangeControl` fields) while `lens`/`iso` are shown in their **collapsed** summary state — meaning at most one (or a user-chosen few) Facet is expanded at a time, not all eight simultaneously visible in full form. A collapsed Facet's summary value reads `"All {Facet}"` when unbounded/default, or the current value/range once set (e.g. `"24mm"`, an eventual `"100–400"` for ISO).
- **8 Facets total, mapped to `Photo` fields:** `lens` → `lensLabel`, `date` → `capturedAt`, `iso` → `iso`, `aperture` → `apertureF`, `shutter` → `shutterSpeedSec`, `exposureComp` → `exposureCompEv` (the one field that's a Facet, never an Insights histogram row, per UX-DR4), `megapixelMode` → `megapixelMode`, `camera` (front/rear) → `camera`.
- **`[ASSUMPTION]` Undefined-field handling under an active range/discrete filter:** if a photo's relevant field is `undefined` (a per-field gap on an otherwise-readable photo) and that Facet's filter is currently non-default/active, the photo doesn't match (excluded — there's no value to confirm it satisfies the filter). If that Facet's filter is at its default/unbounded state, the photo is unaffected by it regardless of whether the field is defined. Neither the PRD nor the architecture states this explicitly; it's the only behavior consistent with AD-4's "undefined, never a sentinel" rule.
- **`[ASSUMPTION]` Which side is "offending" on an invalid range:** when `min > max`, show the `FieldError` on the **max** field (the more common convention — "must be ≥ min"), not the min field. Flag if a different convention is intended.
- **AC #5 needs no new mechanism** — it's a direct consequence of Story 3.1's AD-3 import-boundary rule already being in place; if `insights/` genuinely never imports Browse's selectors, this AC holds by construction. Don't add a manual "notify Insights" mechanism.
- **AC #4's "no Apply button" means every control's `onChange` commits directly to `useFacetFilters()`'s store slot** — there is no separate local/pending filter draft state to sync later.

## Tasks / Subtasks

- [x] Task 1: `Range-control` (AC: #2, #3) — `apps/gallery/src/features/browse/RangeControl.tsx`
  - [x] Two underline `Field`s (Story 1.4) sharing one `data-label` row — `min`/`max` for numeric Facets, `from`/`to` for the date Facet
  - [x] Either side blank means unbounded on that side
  - [x] If `min > max` (both sides filled): show `FieldError` on the max field, and do not commit this Facet's filter until corrected (previous valid value, or unbounded, stays active in the meantime)
- [x] Task 2: `Facet-panel` shell (AC: #1)
  - [x] Desktop: renders inside Story 3.2's reserved sidebar region (`260px` fixed, `--m-panel` background, 2px `--m-dim` border, 20px padding, 24px gap between fields — per Story 3.2's mockup-confirmed values)
  - [x] Each of the 8 Facet fields renders as a collapsed summary trigger (`data-label` + current value + chevron) that expands to its real control on interaction, per Dev Notes
  - [x] Mobile: DEFERRED — user decision 2026-07-08: this iteration focuses on desktop only; the sidebar is hidden below `lg` instead of a slide-up sheet (see deferred-work.md)
  - [x] No presence on the Insights tab (already true by construction — the panel only renders inside the Browse panel's tree)
  - [x] "Clear filters" button uses `Button variant="outline"` (matches the mockup's `.btn-outline` styling), placed at the bottom of the panel
- [x] Task 3: Discrete Facet controls (AC: #2)
  - [x] `lens`: `Select`, single-select, options = distinct `lensLabel` values present in the full readable set (via a dedicated `useLensOptions()` selector, not a direct `useReadablePhotos()` call from `browse/` — keeps AD-3's import-boundary spirit)
  - [x] `megapixelMode`: `RadioGroup` — options `All` (default) / `12 MP (standard)` / `48 MP (ProRAW)`
  - [x] `camera`: `RadioGroup` — options `All` (default) / `Front (selfie)` / `Rear`
- [x] Task 4: Range Facet controls (AC: #2, #3)
  - [x] `date`, `iso`, `aperture`, `shutter`, `exposureComp` each get a `RangeControl` instance, wired to the corresponding `Photo` field per Dev Notes' mapping
- [x] Task 5: Live filtering + Clear-all + mobile count (AC: #4)
  - [x] Add the `facetFilters` store slot and build `useFacetFilters()` for the first time: one entry per Facet, defaulting to unbounded/`All`
  - [x] Replace `useFilteredPhotos()`'s trivial alias body (Story 3.2: `() => useReadablePhotos()`) with real logic that applies every active Facet's predicate against `useReadablePhotos()`, AND-combined (per Dev Notes' undefined-handling rule) — the grid doesn't need to change, it already consumes this selector
  - [x] Every control's `onChange` commits directly to the store — no local draft/Apply step
  - [x] "Clear-all" resets every Facet to its default (unbounded/`All`)
  - [x] `[ASSUMPTION]` Mobile trigger count: DEFERRED along with the rest of mobile scope — no mobile trigger exists this iteration, so no count logic was built (avoids speculative code with no consumer)
- [x] Task 6: Verify (AC: #1–#5)
  - [x] Apply one filter per Facet type (one discrete, one range) — confirm the grid narrows correctly and AND-combines when two are active together
  - [x] Enter an invalid range (min > max) — confirm `FieldError` shows on the max side and the grid doesn't change until corrected
  - [x] Confirm Insights' numbers are identical with filters active vs. cleared
  - [x] Resize to mobile width — DEFERRED, no mobile-specific behavior built this iteration (see Task 2)

## Project Structure Notes

```text
apps/gallery/src/features/browse/
  RangeControl.tsx   # new — Gallery-local
  FacetPanel.tsx      # new — Gallery-local, fills Story 3.2's reserved sidebar region
```

### References

- [Source: planning-artifacts/epics.md#Story 3.3, #UX-DR5, #UX-DR6] — acceptance criteria, component specs
- [Source: ux-designs/ux-BMAD/DESIGN.md#Components — facet-panel, range-control] — sidebar/sheet layout, control-per-Facet-type rule
- [Source: ux-designs/ux-BMAD/mockups/gallery-browse.html] — real reference markup/CSS: lens is single-select (not multi), exact RadioGroup option labels, collapsed/expandable facet-field pattern, "Clear filters" button styling, sidebar dimensions
- [Source: architecture/architecture-BMAD/ARCHITECTURE-SPINE.md#AD-3, #AD-4] — Browse-only selector boundary, per-field `undefined` semantics
- [Source: 3-2-photo-grid-browse-unfiltered-view.md] — the trivial `useFilteredPhotos()` alias and sidebar region this story replaces/fills in

## Dev Agent Record

### Agent Model Used

Claude Sonnet 5 (claude-sonnet-5)

### Debug Log References

- `npx tsc -p tsconfig.json --noEmit`, `npx eslint .` (gallery) — clean throughout implementation.
- `npx turbo run lint build test` (workspace-wide) — 10/10 tasks pass, 57 Vitest tests (20 in `ingestStore.test.ts` incl. 7 new `matchesFacetFilters` cases, 3 new `RangeControl` `isRangeInvalid` cases, 4 new `FacetPanel` `summarizeRange` cases; 30 pre-existing).
- **Live-verified via headless Playwright** against synthetic Pillow-generated EXIF fixtures (4 photos: two 24mm, one 48mm, one front-camera 48MP, one EXIF-less/unreadable): confirmed lens=24mm narrows 3→2 readable cells, AND-combining lens=24mm + ISO≥500 narrows to 1 (the single matching photo), entering an invalid ISO range (max<min) shows the `FieldError` and leaves the grid unchanged (`COUNT_DURING_INVALID_RANGE` stayed at the last valid count), fixing the range re-commits and updates the grid, Clear-filters resets to all 3, Camera=Front(selfie) narrows to 1, and Insights' "3 readable photos" text is identical whether Browse's lens filter is active or not (AC #5) — zero console errors after the Select fix below.
- **Found and fixed a real, pre-existing bug in `packages/ui/src/Select/Select.tsx`** during live verification: clicking a non-focusable `<li role="option">` fires a native `mousedown` that blurs the trigger button first (`relatedTarget` resolves to `null`), which fired `handleContainerBlur` → `setIsOpen(false)` → unmounted the option *before* the browser's subsequent `click` event could reach its `onClick` handler. Result: mouse-driven option selection silently did nothing (only keyboard `Enter`/`Space` selection worked) — confirmed via a debug `console.log` in `commitOption` that never fired on a real click. This is Select's first production consumer outside Storybook (previously only Storybook + keyboard-nav testing exercised it), so the bug had gone uncaught. Fixed with the standard pattern: `onMouseDown={(e) => e.preventDefault()}` on each `<li>`, which stops the browser from shifting focus away from the trigger, so the blur/close-before-click race never triggers.
- **Post-review fix-up round (2026-07-08):** `npx turbo run lint build test` — 10/10 tasks pass, 69 Vitest tests (9 new in `RangeSlider.test.ts`, 3 new `computeBounds` cases in `ingestStore.test.ts`). Fresh headless Playwright run against the same synthetic fixtures confirmed: Browse is the default active tab, the Facet-panel sidebar is visible and identical on both tabs, applying `lens=24mm` changes Insights' "N matching photos" text from 3→2 (proving the global-filter wiring) as well as the Browse grid, no duplicate "MEGAPIXEL MODE"/"LENS / FOCAL LENGTH" text once expanded, the ISO slider renders 2 `role="slider"` thumbs and a keyboard `ArrowLeft×5` on the max thumb moves the MAX field from 800→795 (and narrows the grid accordingly), and the Date-range Fields render one below the other (`toBox.y > fromBox.y`) instead of overflowing side-by-side. Zero console errors after the infinite-render fix (see Completion Notes).

### Completion Notes List

- **`RangeControl.tsx`** (Gallery-local): two `Field`s sharing one `data-label` row, generic over `type="number"|"date"` (date compares `YYYY-MM-DD` strings lexicographically — no `Date` parsing, consistent with `normalize.ts`/`aggregations.ts`'s existing string-slice convention). Holds local draft state that only re-syncs from props when they change *externally* (e.g. Clear-all), so a mid-edit invalid range never gets clobbered mid-keystroke. `isRangeInvalid` extracted as a pure, exported, unit-tested function (AC #3).
- **`FacetPanel.tsx`** (Gallery-local, fills Story 3.2's reserved sidebar): all 8 Facets behind an independent-per-field `FacetField` collapse wrapper (data-label + summary + `▾`/`▲` chevron — plain unicode character, not a heroicon, since `apps/gallery` has no direct `@heroicons/react` dependency and adding one for a single glyph wasn't worth a new dependency). `lens` uses `Select` (single-select, no `multiple`); `megapixelMode`/`camera` use `RadioGroup` with the mockup's exact option labels including the checked-by-default `All`; the 5 range Facets each get a `RangeControl` instance. Every `onChange` commits straight to the store (no local Apply step, AC #4). "Clear filters" is `Button variant="outline"`.
- **Store additions to `ingestStore.ts`:** `FacetFiltersState`/`RangeFilter` types, `DEFAULT_FACET_FILTERS` (all-unbounded), `useFacetFilters()`, `setFacetFilter()`/`clearAllFacetFilters()` mutators, and the pure `matchesFacetFilters()` predicate (AND-combines all 8 Facets, unit-tested with 7 cases covering discrete/range/date matching and the undefined-field-under-an-active-filter exclusion rule from Dev Notes). `useFilteredPhotos()`'s trivial Story 3.2 alias is now real: filters `state.photos` by `readable && matchesFacetFilters(...)` in one selector (not by calling `useReadablePhotos()` from inside another hook, to stay a single store subscription). Added `useLensOptions()` — a purpose-built selector over the *full* readable set (not `useFilteredPhotos()`) so the lens dropdown's own option list doesn't shrink away as other Facets narrow the grid, and so `browse/` doesn't need to call `useReadablePhotos()` directly (keeping AD-3's "browse's only input is `useFacetFilters()`/`useFilteredPhotos()`" convention intact even for this one auxiliary read).
- **Mobile scope (AC #1's slide-up sheet, AC #4's mobile trigger count, Task 2/5/6's mobile bullets): deferred by user decision, 2026-07-08** — this iteration targets desktop only. `Browse.tsx`'s `<aside>` is `hidden lg:block` (not stacked full-width like Story 3.2's placeholder was) since a fully-interactive, un-sheeted Facet-panel stacked awkwardly above the grid would be a worse experience than simply not showing it below `lg`. See deferred-work.md for the follow-up.
- AC #5 required no new mechanism, exactly as Dev Notes predicted — verified live (see Debug Log References) rather than assumed.

### Post-review fix-up (2026-07-08, user screenshots)

After the first implementation pass, the user reviewed live screenshots and requested a batch of UI fixes, committed separately per their instruction:

- **Facet-panel made global, not Browse-only** — hoisted from `Browse.tsx` into `App.tsx` as one persistent left sidebar shared by both tabs (previously it lived inside `Browse.tsx` and only existed on that tab). `UnderlineTabs` moved into the right content column alongside the tab panels. **This reverses Story 3.1/3.3's original AC #5 ("Insights remains unaffected by Browse filters") and AD-3's "Insights always shows the full readable set" invariant** — a deliberate user decision, not a bug: `Insights.tsx` now reads `useFilteredPhotos()` instead of `useReadablePhotos()`, so Facet filters narrow both the grid and the aggregate numbers together. `useUnreadableCount()` stays against the raw ingested batch (unreadable photos have no facet fields regardless of filter state). Relevant code comments in `ingestStore.ts` updated to describe the new global model instead of the superseded Browse-only one.
- **Browse and Insights now share one page-width wrapper** — the `mx-auto max-w-container-max px-gutter py-8` wrapper moved from `Browse.tsx`'s own markup up to `App.tsx`'s shared layout; `Insights.tsx` dropped its narrower `max-w-article-max` wrapper entirely. Both tabs now render at identical width (fixes Insights looking narrower than Browse).
- **Browse tab is now first and the default** — `GALLERY_TABS` reordered, `currentTab` initial state changed from `"insights"` to `"browse"` (user: photos are the primary thing to see, stats are a bonus).
- **Fixed a real duplicate-label bug in `FacetField`** — every child control (`Select`/`RadioGroup`/`RangeControl`) already renders its own label/legend, so the collapsed-trigger's own `data-label` text was showing *again* once expanded (e.g. "MEGAPIXEL MODE" twice). Fixed by only rendering the outer `data-label` while collapsed; the expanded state shows just a small collapse-affordance chevron (with an `sr-only` "Collapse {label}" for accessibility) and lets the child control's own label be the only visible one.
- **Added a dual-thumb `RangeSlider`** (new Gallery-local component, `apps/gallery/src/features/browse/RangeSlider.tsx`) for the 4 numeric range Facets (ISO/aperture/shutter/exposure-comp) — user wanted a slider rather than typing two numbers, "or a slider with the two inputs still below it." Built custom (pointer + keyboard driven, drag-either-thumb, click-track-jumps-to-nearest-thumb) rather than the classic two-overlapping-native-`<input type="range">` CSS trick, since that trick needs `::-webkit-slider-thumb`/`::-moz-range-thumb` pseudo-element selectors this repo's Tailwind setup can't reach without arbitrary values (hard-banned). Square thumbs, no rounded corners — matches this design system's zero-radius aesthetic. Pixel↔value mapping (`percentFor`/`valueFromRatio`/`clamp`) extracted as pure, unit-tested functions. Domain bounds come from a new `useNumericFacetBounds()` store selector (same full-readable-set sourcing as `useLensOptions()`, so the slider's scale doesn't shift as filters narrow the grid); a degenerate single-value set widens by 1 so the slider always has a draggable range. **Found and fixed a real infinite-render bug while wiring this in:** the bounds selector's first version returned `{ iso: [min, max], ... }` — `useShallow` only compares one level deep, so the nested tuple arrays (fresh references every call) always compared unequal, causing "Maximum update depth exceeded." Fixed by flattening the selector's return shape to top-level primitives (`isoMin`/`isoMax`/etc.) and building `[min, max]` tuples locally in `FacetPanel.tsx` only when passing them as a plain prop (not through a store-shallow-comparison boundary).
- **Date range's two Fields now stack vertically** instead of side-by-side — native `<input type="date">` (with its calendar-icon affordance) is too wide for two to fit side-by-side in the 260px sidebar without overflowing (user screenshot). `RangeControl` now conditionally applies `flex-col` when `type="date"`; numeric range Facets stay side-by-side.
- Live-verified all of the above via a fresh headless Playwright run (see Debug Log References) — including the infinite-loop regression, which the first attempt caught immediately (blank page, "Maximum update depth exceeded" in console) before the fix.

### File List

- `apps/gallery/src/features/browse/RangeControl.tsx` (new, then modified — min/max Field pair + pure `isRangeInvalid`; later added the optional `RangeSlider` + date-type vertical stacking)
- `apps/gallery/src/features/browse/RangeControl.test.ts` (new — unit tests for `isRangeInvalid`)
- `apps/gallery/src/features/browse/RangeSlider.tsx` (new, post-review fix-up — dual-thumb slider + pure `clamp`/`percentFor`/`valueFromRatio`)
- `apps/gallery/src/features/browse/RangeSlider.test.ts` (new, post-review fix-up — unit tests for the pure slider math)
- `apps/gallery/src/features/browse/FacetPanel.tsx` (new, then modified — the 8-Facet panel + `summarizeRange` helper; later fixed the `FacetField` duplicate-label bug and wired in `RangeSlider`/`useNumericFacetBounds`)
- `apps/gallery/src/features/browse/FacetPanel.test.ts` (new — unit tests for `summarizeRange`)
- `apps/gallery/src/features/browse/Browse.tsx` (modified, then modified again — dropped its own `FacetPanel`/sidebar/page-width wrapper post-review, now just the grid)
- `apps/gallery/src/app-shell/App.tsx` (modified, post-review fix-up — hosts the now-persistent global `FacetPanel` sidebar + `UnderlineTabs` in the right column; `GALLERY_TABS` reordered Browse-first, default tab changed to `"browse"`)
- `apps/gallery/src/features/insights/Insights.tsx` (modified, post-review fix-up — reads `useFilteredPhotos()` instead of `useReadablePhotos()`, dropped its own `max-w-article-max` wrapper, reworded the "no filtering here" copy and the empty-state message)
- `apps/gallery/src/store/ingestStore.ts` (modified — `FacetFiltersState`/`RangeFilter` types, `DEFAULT_FACET_FILTERS`, `useFacetFilters`/`setFacetFilter`/`clearAllFacetFilters`, `matchesFacetFilters`, `useLensOptions`, real `useFilteredPhotos()` body; post-review fix-up added `computeBounds`/`useNumericFacetBounds` and updated stale Browse-only doc comments to describe the new global-filter model)
- `apps/gallery/src/store/ingestStore.test.ts` (modified — added `matchesFacetFilters` test suite; post-review fix-up added `computeBounds` tests)
- `packages/ui/src/Select/Select.tsx` (modified — unplanned fix: `onMouseDown` preventDefault on options to fix a real click-selection bug found via live verification)

### UX designer fix-up round 2 (2026-07-08, Sally / bmad-agent-ux-designer, user screenshots)

The user flagged the fix-up round 1 result as still design-rough and explicitly requested the UX designer agent. Four issues, all fixed:

- **Removed the `FacetField` collapse/expand toggle entirely** (this round's biggest change) — round 1's fix (hide the outer label only while expanded) still left a click-to-reveal interaction the user didn't want: "always show the slider/inputs/checkboxes right away, no switching between a short variant and an expanded one." `FacetField` is now a bare divider (`border-b-2 border-dim pb-4`, no label, no button, no state) — every Facet's real control (which already renders its own label) is always mounted and visible at a fixed position. This also made `summarizeRange()` dead code (it only existed to render collapsed-state summary text), so it and its test file (`FacetPanel.test.ts`) were deleted rather than left unused.
- **Fixed a real bug in `packages/ui/src/Field/Field.tsx`**: the floating-label logic (`isFocused || currentValue.length > 0`) assumes an empty text input renders nothing, but native `type="date"` inputs always render their own `dd.mm.yyyy`-style mask regardless of value/focus — so an empty, unfocused date Field's label stayed in the lowered "resting" position and visually collided with that mask. Fixed by always floating the label for `date`/`time`/`month`/`week`/`datetime-local` types. Shared-component fix, not scoped to this story's own files, but blocking correct rendering of the Date-range Facet.
- **Single page-level scroll region** — previously the `<aside>` and each `role="tabpanel"` div each had their own independent `overflow-y-auto`, producing 2–3 simultaneous scrollbars once the Facet-panel had enough always-visible content to overflow. Removed all of those; the shared content wrapper in `App.tsx` (sidebar + right column together) is now the one scroll container. **Trade-off, accepted deliberately (same category of change as the AD-3/AC #5 reversal above):** switching tabs no longer preserves each tab's own independent scroll position (Story 3.1's original AC #2) — there's one shared scroll position now, not one per tab.
- **Restored the gap between `UnderlineTabs` and the `// BROWSE`/`// INSIGHTS` eyebrow** — lost during the fix-up round 1 restructuring (Insights/Browse's own `py-8` wrapper, which used to provide this spacing, moved up to the shared `App.tsx` layout and stopped applying between the tabs and the panel content specifically). Added `gap-7` (28px) to the right column's flex container instead — verified via Playwright at exactly 28px on both tabs.

Live-verified via a fresh headless Playwright run: all 8 Facets' real controls (2 slider thumbs × 4 numeric Facets, 1 Select combobox, radios) are present and interactive without any prior click; exactly one element on the whole page has `overflow-y: auto` with real overflow (confirmed via a DOM-wide scan), and scrolling it reaches every Facet down to "Clear filters"; the date Field's label no longer overlaps its input; the tabs→eyebrow gap measures 28px on both tabs; filtering (lens=24mm → 2 cells, Clear filters → 3 cells) still works correctly with the always-expanded UI. `turbo lint/build/test` — 10/10 tasks, 65 Vitest tests (69 minus the 4 deleted `summarizeRange` tests). Zero console errors.

### File List — round 2 additions/corrections

- `apps/gallery/src/features/browse/FacetPanel.tsx` (modified again — removed the entire collapse/expand mechanism and `summarizeRange`; `FacetField` is now a bare divider)
- `apps/gallery/src/features/browse/FacetPanel.test.ts` (deleted — tested `summarizeRange`, which no longer exists)
- `apps/gallery/src/app-shell/App.tsx` (modified again — single shared `overflow-y-auto` scroll region instead of per-panel/per-sidebar ones; `gap-7` added between `UnderlineTabs` and the tab-panel content)
- `packages/ui/src/Field/Field.tsx` (modified — unplanned fix: label always floats for `date`/`time`/`month`/`week`/`datetime-local` input types, fixing a real overlap with those types' native placeholder mask)

### UX designer fix-up round 3 (2026-07-08, Sally, user screenshot + explicit redesign request)

The user clarified the scrollbar request meant the *real document/window scrollbar at the viewport edge*, not round 2's centered-column scrollbar — and separately requested a substantive redesign of the numeric/discrete Facet controls: checkboxes with only the exact values present in the ingested photos (no slider positions that don't correspond to real data), for lens/ISO/aperture/exposure-comp/megapixel; shutter keeps its slider but now snaps only to the batch's real discrete shutter speeds; the date Facet simplified to year-only checkboxes.

- **Real document-level scroll** — removed the `overflow-y-auto` from round 2's centered content wrapper entirely; `App.tsx`'s outer shell is now `min-h-screen` (not `h-screen`) with no inner scroll container, so the page grows naturally and the browser's own scrollbar renders at the true viewport edge. `HeaderBar` is wrapped in a `sticky top-0` div (with `var(--m-z-header)`, following the existing inline-style-for-z-index pattern already used by `Select`/`Modal`/`Toaster`) so it stays reachable while the rest of the page scrolls. Verified via a DOM-wide scan: zero elements have both `overflow-y: auto` and actual overflow (`nestedScrollers: []`), and `document.documentElement` itself is the scrollable one.
- **Facet data model changed from ranges/single-values to checkbox-style multi-select arrays** for `lens`, `iso`, `aperture`, `exposureComp`, `megapixelMode`, and the new `years` (replacing `dateFrom`/`dateTo` entirely) — `FacetFiltersState` now holds `string[]`/`number[]` per Facet (empty = inactive/"All"; non-empty = OR-within-facet, AND-across-facets). `shutter` is the one Facet still `RangeFilter`-shaped, driving the discrete-snap slider. `matchesFacetFilters` rewritten around a `matchesDiscrete()`/`matchesYear()` pair (replacing `matchesDateRange`), still applying the same "active filter + undefined field ⇒ excluded" rule from the original Dev Notes.
- **New `useFacetValueOptions()` selector** (replacing both `useLensOptions()` and `useNumericFacetBounds()`) returns the distinct, sorted values actually present for all 7 discrete/slider dimensions, computed over the full readable set (same "don't shrink as filters narrow" reasoning as the old `useLensOptions()`). **Deliberately memoized via React's `useMemo` keyed on `state.photos`'s own reference, not `useShallow` on the computed result** — `computeFacetValueOptions()` returns fresh nested arrays every call, which would reproduce the exact infinite-render bug class fixed earlier in round 1 (`useShallow` only compares one level deep). `state.photos` only changes reference on ingest/commit, so this keeps the hook's output referentially stable across ordinary filter interactions.
- **`RangeControl.tsx`/`RangeControl.test.ts` deleted** — the checkbox/slider redesign left it with zero remaining consumers (Story 3.3's original min/max-Field-pair pattern is gone from every Facet). Consistent with this story's existing precedent of deleting dead code (`summarizeRange` in round 2) rather than leaving it unused.
- **`ShutterFacet`** (new, inline in `FacetPanel.tsx`): wraps the existing `RangeSlider` but drives it by *index* into the batch's sorted distinct shutter-speed values (`min=0, max=values.length-1, step=1`) instead of a continuous numeric domain, so the slider can only land on speeds that actually occur; a text readout below shows the selected range in photographic fraction notation (`formatShutterSpeed`, e.g. `"1/500s"`), unit-tested.
- **`CheckboxFacetGroup`** (new, generic, inline in `FacetPanel.tsx`): renders a `Checkbox` per distinct value for lens/ISO/year/aperture/exposure-comp/megapixel, with per-Facet label formatters (`f/{value}` for aperture, `+{value}`/`{value}` for exposure comp per photographic convention — `formatExposureComp`, unit-tested — plain value for ISO/lens/year, existing descriptive strings for megapixel). Shows "No data for this batch." when a dimension has zero distinct values, matching Insights' existing empty-dimension convention.
- **Camera stayed a `RadioGroup`** (single-select All/Front/Rear) — not mentioned in the user's checkbox-conversion list, left unchanged.

Live-verified via a fresh headless Playwright run against 5 new synthetic EXIF fixtures with deliberately overlapping values (shared lens/ISO across some photos): every checkbox count matched the exact distinct-value count computed from the data (lens 3, ISO 4, year 2, aperture 3, exposure-comp 3, megapixel 2); the shutter slider still showed exactly 2 thumbs with a correct `"1/500s – 1/30s"`-style readout; zero native `type="date"` inputs remained; multi-select-within-a-Facet (OR) combined correctly with AND across Facets (checking `lens=24mm` → 3 cells, additionally checking `iso=200` → 2 cells, additionally checking `lens=48mm` too → still 2 cells since ISO stayed the binding constraint); Clear filters correctly reset to all 5; zero console errors. `turbo lint/build/test` — 10/10 tasks, 69 Vitest tests.

### File List — round 3 additions/corrections

- `apps/gallery/src/app-shell/App.tsx` (modified again — removed the round-2 inner `overflow-y-auto`; outer shell is `min-h-screen` with no scroll constraint, real document-level scroll; `HeaderBar` wrapped in a `sticky top-0` div)
- `apps/gallery/src/features/browse/FacetPanel.tsx` (rewritten — `CheckboxFacetGroup`/`ShutterFacet` replace `RangeControl`-based Facets for 6 of 8 dimensions; `formatShutterSpeed`/`formatExposureComp` pure helpers added)
- `apps/gallery/src/features/browse/FacetPanel.test.ts` (rewritten — tests `formatShutterSpeed`/`formatExposureComp` instead of the deleted `summarizeRange`)
- `apps/gallery/src/features/browse/RangeControl.tsx` (deleted — zero remaining consumers after the checkbox/slider redesign)
- `apps/gallery/src/features/browse/RangeControl.test.ts` (deleted — tested the deleted `RangeControl`)
- `apps/gallery/src/store/ingestStore.ts` (modified again — `FacetFiltersState` reshaped to checkbox-style arrays + `years` replacing `dateFrom`/`dateTo`; `toggleInArray`, `matchesDiscrete`, `matchesYear` added; `useLensOptions`/`useNumericFacetBounds`/`computeBounds` replaced by `computeFacetValueOptions`/`useFacetValueOptions`)
- `apps/gallery/src/store/ingestStore.test.ts` (modified again — `matchesFacetFilters` suite rewritten for the discrete-array model; `computeBounds` tests replaced with `computeFacetValueOptions`/`toggleInArray` tests)

### UX designer fix-up round 4 (2026-07-08, Sally, user screenshot + follow-up requests)

Four more items from the user, after round 3 landed: the slider thumbs visually overflowed past the sidebar's content edge; lens/aperture should also be slider-driven (not checkboxes) using the same "only real data values" rule; every slider-driven Facet needed a toggle between picking one exact value and picking a range; Camera should be the first Facet in the list.

- **Slider thumbs now flush with the component's own edges** — at 0%/100% a thumb (centered via `-translate-x-1/2` on its percent position) previously overhung by half its own width past the track's visual ends. Fixed by wrapping both `RangeSlider` and the new `SingleSlider` in `px-2` (8px, exactly half the `size-4`/16px thumb) so the thumb's *outer* edge lands flush with the component's bounding box instead of overflowing past it. `trackRef` and both thumbs now share that one padded coordinate space, so pointer-position math and visual position stay in sync.
- **Lens and aperture converted from checkbox lists back to `RangeFilter`-shaped sliders**, reusing the exact discrete-index-driven pattern already built for shutter in round 3 — `matchesRange` (already generic) needed zero changes; `lens` matches against the integer parsed off `lensLabel`'s `"{n}mm"` shape (not `focalLengthMm` directly), so the slider can never land on a value more precise than what's actually displayed. `FacetValueOptions.lens` changed from `string[]` to `number[]` accordingly.
- **New single-value/range toggle** for all three slider-driven Facets (lens, aperture, shutter) — a small button next to each Facet's label switches between a new one-thumb `SingleSlider` (exact value, `min === max`) and the existing two-thumb `RangeSlider`. No store-level change was needed: `matchesRange` already treats `min === max` as an exact match. Switching *to* single mode collapses the filter to the lower thumb's current value; switching *to* range mode leaves the filter as a zero-width range at the current single value, for the user to then drag apart.
- **Camera reordered to the first Facet** in the sidebar list (was previously last, right before "Clear filters").

Live-verified via a fresh headless Playwright run: Camera confirmed as the first `text-data-label`/`legend` on the page; 6 slider thumbs present by default (lens + aperture + shutter × 2 each); thumb bounding-box `left` matched the `RangeSlider`'s own outer wrapper `left` exactly (flush, not the sidebar's overall padding edge — a 2px difference there was traced to the sidebar's own 2px border, not a slider misalignment); toggling lens to "Pick one" dropped the thumb count to 5 and rendered a single blue thumb; moving it via keyboard (`ArrowRight`, 24mm → 35mm) correctly narrowed the grid to the one matching photo. `turbo lint/build/test` — 10/10 tasks, 70 Vitest tests. Zero console errors.

### File List — round 4 additions/corrections

- `apps/gallery/src/features/browse/RangeSlider.tsx` (modified — flush-edge `px-2` wrapper on `RangeSlider`; new `SingleSlider` export sharing the same pure helpers)
- `apps/gallery/src/features/browse/FacetPanel.tsx` (rewritten — generic `SliderFacet` (single/range toggle) replaces the shutter-only `ShutterFacet`, now also used for lens/aperture; `CheckboxFacetGroup` narrowed to `T extends number`; Camera moved first)
- `apps/gallery/src/store/ingestStore.ts` (modified again — `FacetFiltersState.lens`/`.aperture` changed from arrays back to `RangeFilter`; `FacetValueOptions.lens` changed from `string[]` to `number[]`; `parseLensLabel` helper added; `matchesFacetFilters` updated to use `matchesRange` for lens/aperture)
- `apps/gallery/src/store/ingestStore.test.ts` (modified again — lens/aperture test cases rewritten for the `RangeFilter` model, incl. a single-value/`min === max` case)

### UX designer fix-up round 5 (2026-07-08, Sally, user follow-up)

Four more items after round 4: the "Pick one"/"Pick range" text toggle didn't fit next to longer labels; Camera's Front/Rear options should disable (not just silently do nothing) when the batch has no matching photos; ISO should join lens/aperture/shutter as a slider; and a new facet order.

- **Icon-based mode toggle** — replaced the text button with `SliderModeToggle`, a compact 2-icon segmented control (`•` for single value, `↔` for range), both always visible with `aria-pressed` marking the active one. Fixes the text-overflow problem directly (fits in a fraction of the space) and makes the current mode unambiguous without reading text (a single state-flipping icon button wouldn't have been).
- **Camera options disable with zero matches** — `packages/ui`'s `RadioGroup`/`RadioOption` gained an optional per-option `disabled` field (previously only a whole-group `disabled` prop existed); `FacetValueOptions` gained `hasCameraFront`/`hasCameraRear` booleans (computed the same full-readable-set pass as everything else), and `FacetPanel` disables "Front (selfie)"/"Rear" individually when the corresponding boolean is false. Camera's options are the one Facet that isn't data-derived (fixed All/Front/Rear), so disabling instead of omitting was the only available lever.
- **ISO converted to a `SliderFacet`**, identical pattern to lens/aperture/shutter — `FacetFiltersState.iso` changed from a checkbox-style array back to `RangeFilter`, matched via the existing generic `matchesRange` (zero new matching logic).
- **Facet order**: Camera, Year, Lens / focal length, Aperture, Shutter speed, ISO, Exposure comp, Megapixel mode.

Live-verified via a fresh headless Playwright run: DOM order of Facet labels matched the requested sequence exactly; with a batch containing no front-camera photos, `#facet-camera-front` reported `disabled: true` while `#facet-camera-rear` stayed `false` (re-run with a batch that *did* include a front-camera photo confirmed the same option correctly re-enables); exactly 4 `SliderModeToggle` groups present (lens/aperture/shutter/ISO); zero `#facet-iso-*` checkbox inputs remained. `turbo lint/build/test` — 10/10 tasks, 71 Vitest tests. Zero console errors.

### File List — round 5 additions/corrections

- `packages/ui/src/RadioGroup/RadioGroup.tsx` (modified — `RadioOption` gained an optional per-option `disabled` field, applied alongside the existing group-level `disabled` prop)
- `apps/gallery/src/features/browse/FacetPanel.tsx` (rewritten again — new `SliderModeToggle` icon control replaces the text button; ISO moved from `CheckboxFacetGroup` to `SliderFacet`; Camera's Front/Rear options wired to `options.hasCameraFront`/`hasCameraRear`; facet order changed to Camera/Year/Lens/Aperture/Shutter/ISO/ExposureComp/Megapixel)
- `apps/gallery/src/store/ingestStore.ts` (modified again — `FacetFiltersState.iso` changed from array back to `RangeFilter`; `FacetValueOptions` gained `hasCameraFront`/`hasCameraRear`; `matchesFacetFilters` updated to use `matchesRange` for iso)
- `apps/gallery/src/store/ingestStore.test.ts` (modified again — iso test cases rewritten for the `RangeFilter` model; new `hasCameraFront`/`hasCameraRear` coverage)

### UX designer fix-up round 6 (2026-07-08, Sally, user follow-up)

Five more items after round 5: a single-value Facet's range mode was pointless and confusing (e.g. "50mm – 50mm"); the icon toggle should be one button with a muted fill, not two bordered ones; Camera should disappear entirely when the batch has no camera data at all (not just disable its options); Year should join the slider family for consistency; and when Camera *is* shown, it should lay out as one compact row with small labels underneath instead of a vertical list.

- **Range mode locked out entirely when a Facet has ≤1 distinct value** — `SliderFacet` now derives `canRange = values.length > 1`; when false, `effectiveMode` is always `"single"` regardless of any stored state, and the mode toggle is `disabled`. No more degenerate `"50mm – 50mm"` range display.
- **Single toggle button, not two** — round 5's 2-icon segmented control (`SliderModeToggle`) is now one button showing the *current* mode's glyph (`•`/`↔`); clicking flips to the other mode. Styling changed from `border-2` to a `bg-dim` fill with no border, per user request ("without an outline, but with a light muted background").
- **Camera hidden entirely with zero camera data** — `FacetPanel` now checks `options.hasCameraFront || options.hasCameraRear` before rendering the Camera `FacetField` at all (previously it always rendered, just with both options disabled if neither had data) — avoids a Facet occupying sidebar space for a dimension the batch has literally no data on.
- **Year converted to a `SliderFacet`**, joining lens/aperture/shutter/ISO's pattern — `FacetFiltersState.years` changed from a checkbox-style array back to `RangeFilter`; the separate `matchesYear` helper was deleted, replaced by a `parseYear` helper feeding the same generic `matchesRange` everything else already uses.
- **New Gallery-local `CameraFacet`** replaces the shared `RadioGroup` for this one Facet — a single horizontal row (`flex gap-5`) with each option's small label rendered *below* its radio input, instead of `RadioGroup`'s vertical label-beside-input stack. Kept local rather than added as a `RadioGroup` layout variant, since this compact row-with-label-below arrangement is specific to this one Facet's space constraints, not a general design-system pattern (yet).

Live-verified via a fresh headless Playwright run: a batch with two photos sharing an identical 50mm focal length (and no `LensModel` EXIF tag at all, so `camera` stays `undefined` for both) showed zero `<legend>CAMERA</legend>` elements (Camera hidden), a disabled Lens mode-toggle button, and a plain `"50mm"` readout (not a range); the same batch's still-varying Facets (shutter, ISO) kept their toggles enabled and defaulted to range mode. A separate batch with both front- and rear-camera photos rendered Camera as one compact `ALL / FRONT / REAR` row with labels underneath, and clicking "Front" correctly narrowed the grid to the one matching photo. `turbo lint/build/test` — 10/10 tasks, 71 Vitest tests. Zero console errors.

### File List — round 6 additions/corrections

- `apps/gallery/src/features/browse/FacetPanel.tsx` (rewritten again — `SliderModeToggle` reduced to one icon button, muted-background styling; `SliderFacet` gained `canRange`/forced-single-mode logic; new `CameraFacet` component; Year moved from `CheckboxFacetGroup` to `SliderFacet`; `RadioGroup` import removed)
- `apps/gallery/src/store/ingestStore.ts` (modified again — `FacetFiltersState.years` changed from array back to `RangeFilter`; `matchesYear` deleted, replaced by `parseYear` + the existing generic `matchesRange`)
- `apps/gallery/src/store/ingestStore.test.ts` (modified again — year test case rewritten for the `RangeFilter` model)

### UX designer fix-up round 7 (2026-07-08, Sally, real bug + two follow-up requests)

The user caught a genuine bug (not a preference): a slider Facet with multiple possible values, dragged into a collapsed `min === max` state, kept showing the range icon (`↔`) with what looked like a single thumb (actually two thumbs stacked exactly on top of each other) and a degenerate `"28mm – 28mm"` readout — round 6's fix only covered the "dataset has ≤1 possible value" case, not "the currently *selected* range collapsed to one point." Plus two more layout requests: Camera's row should stretch across the sidebar's full width, and the divider lines between Facet sections should go away.

- **Root-caused and fixed the collapsed-range bug**: `SliderFacet` previously tracked its single/range mode as independent `useState`, only forced to `"single"` when `values.length <= 1`. That state could disagree with the actual filter whenever `min`/`max` converged to the same value through normal dragging — the exact scenario in the user's screenshot. Fixed by **deleting the `useState` entirely** and deriving `isSingle` fresh every render: `!canRange || minValue === maxValue`. The displayed mode can now never disagree with what's actually being matched, because it's computed from the same values used to filter. Toggling is now a single `handleToggle()` that reads the current derived state and calls `onChange` accordingly (collapse to `minValue,minValue`, or expand to the full `firstValue,lastValue` range) — no local state to keep in sync at all.
- **`CameraFacet`'s row now spans the sidebar's full width** — `flex gap-5` (fixed gutter, left-clustered) changed to `flex justify-between` (options spread edge-to-edge, first at the left, last at the right).
- **Divider lines between Facet sections removed** — the `FacetField` wrapper (`border-b-2 border-dim pb-4`) added nothing else beyond the divider itself once its own label was dropped in round 5, so it was deleted outright rather than just having its border stripped; every Facet now renders directly as a child of the sidebar's `gap-6` flex column, which alone provides the spacing.

Live-verified via a fresh headless Playwright run: zero elements with `border-b-2`+`border-dim` remain inside the Facets sidebar; Camera's first/last option bounding boxes touch the row's own left/right edges; reproduced the exact bug by focusing a range Facet's max thumb and pressing `ArrowLeft` until it met the min thumb — the number of `role="slider"` elements with `aria-label="Value"` (single-mode) went from 0 to 1 and the matching `aria-label="Maximum"` (range-mode) count dropped by exactly one, confirming the Facet auto-switched to single mode the instant the range collapsed, with no possibility of the icon/thumb-count mismatch recurring. `turbo lint/build/test` — 10/10 tasks, 71 Vitest tests. Zero console errors.

### File List — round 7 additions/corrections

- `apps/gallery/src/features/browse/FacetPanel.tsx` (rewritten again — `FacetField` deleted (no divider lines); `SliderFacet`'s mode `useState` deleted, `isSingle` now derived from `filter` each render; `CameraFacet`'s row uses `justify-between` instead of a fixed `gap-5`)

### UX designer fix-up round 8 (2026-07-08, Sally, user re-raised a round-6 request)

The user pointed out (a third time) that Camera was still showing up with Front or Rear visibly disabled whenever the batch only had photos from *one* of the two — round 6's fix only hid Camera when the batch had *neither* front nor rear data, not when it had just one. Restated plainly: if there isn't a genuine two-way choice, "All" already equals the one value that exists, so offering Camera at all is pointless clutter.

- **Tightened the visibility condition** — `FacetPanel` now renders `CameraFacet` only when `options.hasCameraFront && options.hasCameraRear` (both present), not `||` (either present). With only one camera type in the batch, "All" and that type filter to the identical photo set, so there's no real decision for the user to make.
- Since Camera now only ever renders when both options are genuinely selectable, `CameraFacet` no longer needs its per-option `disabled`/`hasFront`/`hasRear` plumbing at all — simplified back to a plain 3-option row (`CAMERA_OPTIONS` constant), removing the now-dead disabled-styling branches.

Live-verified via a fresh headless Playwright run: a batch with only rear-camera photos (no front at all) now renders zero `<legend>CAMERA</legend>` elements — Year becomes the first Facet in the sidebar, matching the round-6-hidden-Camera screenshot the user originally asked for; a batch with both front- and rear-camera photos present still renders Camera normally. `turbo lint/build/test` — 10/10 tasks, 71 Vitest tests. Zero console errors.

## Change Log

- 2026-07-08: Implemented Story 3.3 — `RangeControl`, `FacetPanel` (8 Facets behind collapse triggers), `facetFilters` store slot + AND-combined `matchesFacetFilters`, `useLensOptions`. Mobile deferred (desktop only this iteration). Found and fixed a real pre-existing `Select` click-selection bug. `turbo lint/build/test` all pass (10/10 tasks, 57 Vitest tests, 27 new).
- 2026-07-08: Post-review fix-up batch (separate commit, user request from screenshots) — Facet-panel made global (persistent sidebar shared by both tabs, Insights now filter-aware too, reversing the original Browse-only/AC #5 design), Browse+Insights share one page width, Browse is now the default tab, fixed a `FacetField` duplicate-label bug, added a dual-thumb `RangeSlider` for the 4 numeric range Facets, fixed the Date-range Fields overflowing (now stacked vertically). Found and fixed a real infinite-render bug in the new slider-bounds selector during this pass. `turbo lint/build/test` re-verified clean (10/10 tasks, 69 Vitest tests); live-verified via a fresh headless Playwright run.
- 2026-07-08: UX designer fix-up round 2 (separate commit, Sally/bmad-agent-ux-designer, user request from further screenshots) — removed the Facet-field collapse/expand toggle entirely (always show the real control), fixed a real `Field` label-overlap bug for date-family input types, consolidated to a single page-level scrollbar (trading away Story 3.1's original per-tab independent scroll-position preservation, a deliberate accepted trade-off), restored the lost gap between the tabs bar and the tab content eyebrow. `turbo lint/build/test` re-verified clean (10/10 tasks, 65 Vitest tests); live-verified via a fresh headless Playwright run.
- 2026-07-08: UX designer fix-up round 3 (separate commit, Sally, user request) — real document-level scroll (round 2's was a centered-column scrollbar, not the viewport edge the user meant), and a substantive Facet-control redesign: lens/ISO/aperture/exposure-comp/megapixel became checkbox-style multi-select lists showing only the exact values present in the ingested photos, shutter kept its slider but now snaps only to real discrete shutter speeds, and the date Facet simplified to year-only checkboxes (replacing `dateFrom`/`dateTo`). `RangeControl` deleted (zero remaining consumers). New `useFacetValueOptions()` selector, deliberately `useMemo`-based (not `useShallow`) to avoid reproducing round 1's infinite-render bug class. `turbo lint/build/test` re-verified clean (10/10 tasks, 69 Vitest tests); live-verified via a fresh headless Playwright run against 5 new fixtures with deliberately overlapping values.
- 2026-07-08: UX designer fix-up round 4 (separate commit, Sally, user request) — slider thumbs now render flush with the component's own edges instead of overhanging past them (`px-2` inset, half the thumb's width); lens and aperture converted from checkboxes back to `RangeFilter`-shaped sliders (reusing shutter's discrete-index pattern, `matchesRange` unchanged); every slider-driven Facet (lens/aperture/shutter) gained a single-value/range toggle (new `SingleSlider` component, no store-level change needed since `min === max` already means an exact match); Camera reordered to the first Facet in the sidebar. `turbo lint/build/test` re-verified clean (10/10 tasks, 70 Vitest tests); live-verified via a fresh headless Playwright run.
- 2026-07-08: UX designer fix-up round 5 (separate commit, Sally, user follow-up) — the single/range toggle switched from a text button to a compact icon segmented control (fixes it not fitting next to longer labels); Camera's Front/Rear options now disable individually when the batch has zero matching photos (new per-option `disabled` on `packages/ui`'s `RadioGroup`); ISO converted to a slider (same pattern as lens/aperture/shutter); Facet order changed to Camera, Year, Lens, Aperture, Shutter, ISO, Exposure comp, Megapixel. `turbo lint/build/test` re-verified clean (10/10 tasks, 71 Vitest tests); live-verified via a fresh headless Playwright run.
- 2026-07-08: UX designer fix-up round 6 (separate commit, Sally, user follow-up) — range mode locked out entirely for any Facet with ≤1 distinct value (no more degenerate "50mm – 50mm"); the icon toggle reduced to one button with a muted-fill style (was two bordered icons); Camera now hides entirely (not just disables its options) when the batch has zero camera data at all; Year converted to a slider, joining lens/aperture/shutter/ISO; new Gallery-local `CameraFacet` lays out as one compact row with small labels underneath when shown, replacing `packages/ui`'s `RadioGroup` for this one Facet. `turbo lint/build/test` re-verified clean (10/10 tasks, 71 Vitest tests); live-verified via a fresh headless Playwright run.
- 2026-07-08: UX designer fix-up round 7 (separate commit, Sally, real bug + follow-up) — fixed a real bug: a range Facet dragged to a collapsed `min === max` state kept showing the range icon with two invisibly-stacked thumbs instead of switching to single mode, because mode was tracked as separate `useState` that could desync from the filter; fixed by deleting that state and deriving the displayed mode directly from `filter` every render. Camera's row now spans the sidebar's full width (`justify-between`). Divider lines between Facet sections removed entirely (the `FacetField` wrapper deleted). `turbo lint/build/test` re-verified clean (10/10 tasks, 71 Vitest tests); live-verified via a fresh headless Playwright run that reproduced and confirmed the fix for the original bug.
- 2026-07-08: UX designer fix-up round 8 (separate commit, Sally, user re-raised round 6's request) — Camera now hides whenever the batch doesn't have *both* front and rear camera photos, not just when it has neither; with only one camera type present, "All" and that type already filter to the same set, so Camera offered no real choice. `CameraFacet` simplified back to a plain 3-option row since the disabled-option case can no longer occur. `turbo lint/build/test` re-verified clean (10/10 tasks, 71 Vitest tests); live-verified via a fresh headless Playwright run.
